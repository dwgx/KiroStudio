<!-- HISTORICAL-ARCHIVE-MARK -->
> ⚠️ **这是过程记录，不是当前状态。** 当前状态看仓根 `STATUS.md`（唯一真相源）。
>
> 本文件已确证含**过期断言**。历史上多次出现「后来的会话把过期断言当约束」而做出错误决策
> ——最严重的一次：一句无依据的「`q.*` 已停用」注释直接导致了一次错误的架构迁移，
> 改坏 region 探测让 US 号恒 403，上线后才发现并回滚。
>
> 读本文件时：**任何数字（测试数 / 配置值 / 池容量 / 行号）一律现读现验**，
> 结论性断言先按 `STATUS.md` 核一遍。本文件的价值在于**依据与推导过程**，不在于它的结论。

---

# KiroStudio 现状总账 — 2026-08-05 01:00

> 一份「做了什么 / 还差什么 / 哪些是我证伪的」的完整清单。
> 前置阅读：`HANDOFF-2026-08-04-NIGHT.md`（第 4.5 节是 429 机制的实测结论）。

---

## 0. 一句话

线上 `a6f7f5fde259c50a`，1 实例 / 0 panic / **0 禁用**，近 15 分钟 **92.5% 成功、0 个 429**。
1105 测试全绿、clippy 0 error、tsc 干净。**七批改动零空窗上线，每条都做过「改回旧行为 →
测试必须 FAILED → 再还原」验证。**

---

## 1. 已上线并验证（七批）

| # | 改动 | 旧行为的具体伤害 |
|---|---|---|
| 1 | **刷新失败按错误类型分流**（`is_refresh_error_credential_level` + `report_refresh_failure_classified`） | `refresh_token_locked` 内部**已对 5xx/网络退避重试 3 次**才上报一次，所以上报里上游抖动占绝大多数。旧代码无条件计数 ⇒ **一次 30s token 端点抖动 = 3 次计数 = 永久禁用 + 落盘**。429 现在明确判瞬态（限流不是号坏） |
| 2 | **`TooManyRefreshFailures` 纳入自愈** | 它与 `InvalidRefreshToken` 是两个信号，此前一起被排除 ⇒ 全池被它打满后**自愈也救不回来**，必须人工去面板点启用 |
| 3 | **`acquire_context_excluding`（排除集）** | 此前唯一阻止 failover 重选刚失败号的是冷却硬门 ⇒ **`cooldownEnabled=false` 时 failover 事实上不存在**。线上该值正是 false，且今晚被改回两次。现在是**结构保证**，与配置无关 |
| 4 | **停机 + 首次成功强制落盘** | `has_ever_succeeded` 读的是从 stats 恢复的 `success_count`，而它是 provider 判「bearer-invalid 403 = 瞬态 or 真 region 错配」的唯一判据。今日 **41 次 SIGTERM 里 39 次走到 SIGKILL** ⇒ 成功记录丢失 ⇒ 重启后健康号被判「从未成功过」⇒ 三次瞬态 403 即禁用。**实测 20:20:30 启动、20:20:32 打死一个 93.9% 成功率的号** |
| 5 | **`region_probe` 接线** + 启动后台串量回填 | 模块早写好、8 测试全绿，但**从未被任何代码调用**。只接 `add_credential` 救得了新号、救不了池里的存量号，故补了启动回填（串行、每号间隔 3s） |
| 6 | **`autoDisableSuspicious` 补齐接线** | 它**只存在于 `config.json` 与 `TokenManager`**：`reload_config` 在读它，但 types/service 完全没接 ⇒ **面板既看不到也改不了**，API 读回 `None`。线上因此得出过错误结论（以为三个自动禁用开关都被关了，其实这一项**改不到**）。已验证可读/可写/可热更 |
| 7 | **按号爬坡限制** + **限流冷却下限** + **REST 端点 US/EU 回退** + **启动 slow-start** | 见第 2、3 节 |

---

## 2. 🔴 429 的真正机制（本轮最重要的发现）

**上游惩罚的是速率的跃升，不是绝对吞吐。**

按「凭据 × 分钟」配对，控制**前一分钟完全无 429**（排除「429 快返回 + shield 重试
⇒ 本分钟计数虚高」这个反向因果）：

| 本分钟 / 前一分钟 | 429 率 |
|---|---|
| **≥5x 跃升** | **48.3%** |
| 2–5x | 5.4% |
| **平稳** | **0.7%** |

与绝对速率交叉制表，每一档内跃升都是主因：**100+ req/min 平缓上量只有 2.9%，
而 <50 req/min 突然跃升有 36.4%。**

### 它一次解释掉全部矛盾观测

- **「同号 kiro-rs 180 RPM 干净、KiroStudio 就限速」** —— 那是逐步加压的压测，全程在「平稳」档
- **#481** 同样 ~90 req/min：20:23 是 0% 429、21:12 是 100% —— 差别在**上一分钟是多少**
- **#507** 在 1~5 req/min 下跑 19 分钟 100% 成功；23:08 因另一个号掉出而瞬间承接
  192 req/min，当场 187 个 429
- **`credentialRpmLimit` 永不触发** —— 它盯的是绝对速率这个错的变量

### 顺带找到的正反馈缺陷

**429 在 ~1s 返回、成功要 3s+**（实测 507 的 429 平均 1062ms / 508 的成功 3264ms）
⇒ 被打爆的号 `inflight` 反而恒低（507 = 1、508 = 13）⇒ 而 `inflight` 是**升序**
排序键 ⇒ **失败越快 → 越显得空闲 → 越被优先选中**。

与 ZyphrZero v0.7.1 那个 `success_count` 补丁（注释原话"劫贫济富"）是同一类缺陷。
修法：`ramp_tier` 插在 `health_tier` 之后、`inflight` 之前，两个位置都有源码守卫。

---

## 3. 与 ZyphrZero kiro.rs v0.7.1 的对照结论

### 他们对 403 的处理**严格比我们差 —— 别照搬**

`provider.rs:620` 的 `401 | 403` 分支直接 `report_failure`（3 次即禁用），账号风控
分支在它**之后**且门控在 `status == 429`，而 `default_account_throttle_kind`
**不认 `temporarily is suspended`**。于是线上占主导的那 3749 次 403 全部落进凭据
失败路径 —— 这正是他们 DB 里 1737 `auth_failed` + 905 全池禁用的来源。

| | KiroStudio（现在） | kiro-rs v0.7.1 |
|---|---|---|
| 403 `temporarily is suspended` | 429 + `Retry-After` + 冷却，**不计失败** | `report_failure`，3 次即禁用 |
| bearer-invalid 403（已成功过的号） | 判瞬态，只冷却 | `report_failure` |
| 风控禁用阈值 | 连续 **6** 次且期间零成功 | 任何 403 计 1，**3** 次即禁用 |

### 已抄的三样

1. **REST 端点 US/EU 双候选回退**（`rest_api_region_candidates`）—— 你的第 ⑤ 点，
   **今晚已实现**。按 SSO region 前缀选主端点、另一个作 403 回退，只对 403 回退
   （401 是 token 真废、429 是限流，换端点无意义，回退只会把失败往返翻倍）。
2. **分级冷却下限**（`scale_floor`）—— `RateLimitExceeded` 8s / `SuspiciousActivity` 12s，
   不受 `cooldownScalePct` 影响。取 8s 而非他们的 60s：我们池子只有 1~3 个号，
   而排除集已保证单请求内换号不依赖冷却；60s × 3 号 = 全池同时冷却 → 网关自造 503，
   正是他们注释里警告的形态。
3. **`PROBE_ORDER` 收窄到 2 项** —— 你的第 ② 点。⚠️ 注意这里**不变量方向反转了**：
   原断言是「上限 < 表长」（意图：别探满），表收窄后那个意图会变成「永远探不到第二个
   候选」⇒ eu 账号拿不到回退机会。故改为「上限 == 表长」，测试注释写明了理由。

### 我们比他们强的两处（你的第 ④ 点，已核实）

- `effective_upstream_region` 优先级多两层：`profileArn` 第 4 段（**严格校验
  `arn:aws:codewhisperer:` 前缀 + region 白名单**）> 凭据 `region`/`auth_region` > config
- `sanitized_region()` + `is_supported_region()`：凭据里的 region 必须命中白名单才用。
  kiro-rs 没有这层，填什么就往什么区打 ⇒ 可拼出 `runtime.{垃圾}.kiro.dev`

---

## 4. 🔴 仍未修复 / 仍存在的问题

### 4.1 shield 与三个 unit 不在任何仓库（最高优先）

`kiro_shield.py`（612 行）+ `kiro-shield.service` + `socks-rotator.service` +
`kiro-ratelimit-monitor.service`，**全部只在 VPS 上 untracked**。

它今日 **4896 resolved / 21156 attempts（4.32× 放大）**，最近一小时
933 wait / 278 resolved / 3 gave up ⇒ **客户端侧成功率 98.9%** —— 它是目前唯一在
吸收 403 的东西。**按现状 clone 重建机器不会有它，客户端会直接吃到全部 403。**
应纳入 ws-vps 仓库。**不要停它。**

### 4.2 ✅ 当前池已无「靠回退」的号（01:00 核实）

```
503  custom_api   （无 region 概念）
507  api_key      apiRegion=us-east-1
509  api_key      apiRegion=eu-central-1
510  api_key      apiRegion=eu-central-1
```

**三个 api_key 号全部有显式 `apiRegion`** ⇒ 定时炸弹在当前池上不成立，
改 `config.region` 不会废掉它们。回填之所以没日志，正是因为
`ids_needing_region_probe` 返回空（已无「无 region 字段」的号）—— 是**运维在上号时
填的**，不是探测跑的。

⚠️ 但这份数据本身证明了「跨区池」是常态：**一个 us-east-1 + 两个 eu-central-1**。
任何单一全局 `config.region` 都会废掉另一半 ⇒ 每凭据显式 region 是唯一正解，
而 `region_probe` 的价值在于**推号方没填 region 时**兜住（实测无 region 号的
「上号即废率」08-02=0% / 08-03=27% / 08-04=30%，在恶化）。

**仍未验证**：`region_probe` 的成功路径从未在生产上被触发过（一直没有符合条件的号）。

### 4.3 两项上线后未被验证

| 项 | 为什么没验证到 |
|---|---|
| **启动 slow-start** | 部署时流量已降到 21~39 req/min，而触发线是 25% × `inboundTargetRpm=300` = **75 RPM** ⇒ 结构上不会触发（排队日志 0 行）。**要等一次高流量时段的重启** |
| **爬坡限制在单号↔多号切换时** | 唯一一次真实观测（#509 于 23:42 入池，`2→2→1→6→18` 五分钟逐步加量、全程 0 个 429）证明**多号可用时有效**；但 23:47 另一号掉出后它瞬间跳到 64（3.5x）就开始 429 |

### 4.4 爬坡限制在单号可用时结构上无效（**用户已决定不做**）

它是**排序键而非硬门**：全池只剩一个号时照常放行，因为拿一个真实 429 好过网关自造 503。
要补需要「超出爬坡额度就在入站返 429 + Retry-After」——吞吐换成功率的取舍，
**用户已明确选择只加重启 slow-start、不加硬限。**

### 4.5 🆕 请求头与 kiro-rs 有 7 处差异（未验证是否影响 429）

在**我们的号实际走的那条路径**（CLI 端点，`q.{region}.amazonaws.com/`，两边同 host）：

| 头 / 字段 | kiro-rs v0.7.1 | KiroStudio |
|---|---|---|
| `x-amzn-codewhisperer-optout` | **`false`** | **`true`** |
| `amz-sdk-request` | `attempt=1; **max=3**` | `attempt=1; **max=1**` |
| `x-amzn-kiro-agent-mode` | 不发 | `vibe` |
| UA `api/` 分隔符 | `codewhispererstreaming**/**0.1.14474` | `codewhispererstreaming**#**1.28.3` |
| UA `os/` | 真实 `system_version` | 硬编码 **`other`** |
| UA `lang/` | `rust**/1.92.0**` | `rust`（无版本） |
| UA `m/` 与两头关系 | `x-amz-user-agent` 用 `m/F`，UA 用 `md/appVersion-{ver}` | **两个头同一个串**，`m/E`，且 **`machineId` 拼进 `appVersion`** |

**这七条我只是读代码比出来的，没有任何实测支撑它们与 429 有关。**
最值得先试的两条（都是单 token 改动、可逆）：`optout` 与 `max=`。
`optout=true` 意味着我们声明「不参与服务改进」，理论上可能对应不同的配额策略；
`max=1` 是向上游声明「本客户端不重试」。**但这是假说，不是结论 —— 需要控制实验。**

另外 IDE 端点两边 host **完全不同**（kiro-rs `q.{region}.amazonaws.com`
vs 我们 `runtime.{region}.kiro.dev`），但我们池里的号是 `endpoint: cli` pinned，
所以这条对当前池子不生效。

### 4.6 已知问题 #20 / #21 原封未动

- **#20**：`acquire_admission()` 超时既不 `emit_record` 也不 bump 计数器 ⇒
  被整形层掐掉的请求在面板上不存在 ⇒ 看到的成功率**偏乐观**
- **#21**：`retries` 无法聚合（`Aggregate` 无 retry 字段、`add()` 不读 `r.retries`）

### 4.7 配置层面

| 项 | 现状 | 说明 |
|---|---|---|
| `cooldownEnabled` | **false** | 今晚被改回两次（20:35、21:5x）。**改动 #3 让 failover 不再依赖它**，所以可以维持 false |
| `autoDisableSuspicious` | false | ⚠️ **与 `cooldownEnabled=false` 是危险组合**：`report_suspicious_activity` 的计数与禁用是**恒执行**的（只有冷却与族级连坐受门控）⇒ 关掉冷却会让更多请求打到已 suspend 的号 ⇒ 403 攒得更快 ⇒ **更容易**撞 6 次阈值。**要开自动禁用就同时开冷却** |
| `inboundQueueTimeoutPassthrough` | true | 排队 5s 后**放行**，整形层仍不拒绝任何东西。改 false 是吞吐换成功率，需拍板 |
| `cooldownScalePct` | 40 | 我 20:52 改的（原 10）。现有 `scale_floor` 兜底，即使被改回 10 也不会低于 8s |
| `credentialRpmLimit` | 230 | 空转（盯的是绝对速率这个错变量）。**不建议按实测改小** —— 单号实测能干净跑 137 RPM |

---

## 5. 我在本轮证伪的六个机制（别重复）

| 假设 | 为什么错 |
|---|---|
| 429 随请求体增大 | **`input_tokens` 来自上游 usage，429 请求永远拿不到 ⇒ 17550/17550 条 `rate_limited` 的 `input_tokens=0`。** 我那张 size-bucket 表只是在区分"失败/成功" |
| 并发数决定 429 | 429 行平均在飞 6.45、成功行 3.51，看着很强。但**只按成功请求算负载**后完全反过来：0 个成功在飞时 429 率 81.6%、5–8 个时 3.4%。那个量测的是「该号是否正处于惩罚窗口」，**是结果不是原因** |
| token 量/分钟有天花板 | #481 单分钟 **2270 万** input tokens 零 429 |
| region 决定 429 | 控制时间后 eu 号同样 55–62%。**region 决定 403，不决定 429** |
| 端点（IDE vs CLI）决定 429 | 两个都干净（454 IDE 0.1% / 473 CLI 0%） |
| **「调度器把 15× 流量发给 97% 失败的号」** | **我自己报错的。** 逐分钟拆开看，507 当时是池里**唯一**的号；508 一入池调度器立刻切过去、507 归零。健康降权本来就在工作。**我读的是跨池子变动的累计数** —— 这个混淆今晚咬了我**六次** |

---

## 6. 流程教训

「回退即 FAILED」验证今晚抓出我**三个纸面测试**：

1. **排除集的行为测试** —— 两道串联 filter 互相掩护，删掉任一条都仍绿。
   拆成「关亲和隔离 filter」+「亲和旁路改源码守卫」，并在注释里写明为什么后者
   只能是源码守卫（`available` 已被前一道 filter 剔过，`find` 必然 `None`）。
2. **首次成功落盘** —— 没预热 debounce 时钟，而 `last_stats_save_at=None` 时
   `save_stats_debounced` 本来就会立刻落盘 ⇒ 删掉修复也绿。已加预热。
3. **`try_take` 是否真用了 boot ramp** —— 拿传入的 1200 算期望值，而 `mk()` 会把它
   clamp 到 `rpm_max=300` ⇒ 两种实现都远小于期望 ⇒ 断言恒真。改为从
   `current_target_rpm()` 取实际生效值后，回退即 FAILED（实得 40 vs 折算后 24）。

**另外：我起的 5 个侦察 agent 全部在启动时就死了**（146 字节占位、零进展）。
上一份交接第 7 节警告过：agent 走的正是生产网关。**本轮所有工作都是直接做的。**

---

## 7. 建议的下一步（按收益排序）

1. **把 shield 与三个 unit 纳入 ws-vps 仓库**（4.1）—— 唯一的「重建即失效」缺口
2. **验证存量号 region 回填真的跑成过**（4.2）—— 查 509/510 有没有 region 字段
3. **试 `optout=false` 与 `max=3`**（4.5）—— 单 token 改动、可逆，但**必须做控制实验**
   （同号、同时间窗、只变一个变量），别按"看起来更像官方客户端"就直接改
4. **等一次高流量重启验证 slow-start**（4.3）
5. #20 的可观测性缺口（面板成功率偏乐观，影响一切后续判断）
