<!-- HISTORICAL-ARCHIVE-MARK -->
> ⚠️ **这是过程记录，不是当前状态。** 当前状态看仓根 `STATUS.md`（唯一真相源）。
>
> 本文件已确证含**过期断言**。历史上多次出现「后来的会话把过期断言当约束」而做出错误决策
> ——最严重的一次：一句无依据的「`q.*` 已停用」注释直接导致了一次错误的架构迁移，
> 改坏 region 探测让 US 号恒 403，上线后才发现并回滚。
>
> 读本文件时：**任何数字（测试数 / 配置值 / 池容量 / 行号）一律现读现验**，
> 结论性断言先按 `STATUS.md` 核一遍。本文件的价值在于**依据与推导过程**，不在于它的结论。

---

# 全部欠账台账（2026-08-06，防遗漏）

> 用户要求：**所有问题不能遗漏**。本文件是唯一进度真相源。
> 每完成一项就把状态改掉，别新开文件（新开文件正是"清单越读越长"的成因）。

## 环境事实（影响排期，已核实）

- **本机 8 核 → workflow 并发上限 6**（`min(16, ncpu-2)`）。派超过 6 个会排队，不会更快。
- `_study/kiro-rs` 与 VPS `/opt/kiro-rs-test` 是**同一份**：ZyphrZero/kiro.rs **v0.7.1**
  （基于 hank9999 二次开发，两处都无 `.git`，是拷贝）`[已核实]`
  ⇒ 本轮拿它当"key 完全没问题"的参考实现是**成立**的。
- 用户上一轮要的是 **`freebattle/kiro.rs`** 的 **OpenAI 处理**研究，与本轮 CLI/region 无关。

---

## A0. 第二轮结果（`wf_913675d7-b14`）+ 主控自己接的部分

**实现 6/6 完成，审查 2/3 已回、均 `sound` 零 must_fix**（第一轮是 3 份有 must_fix，质量提升）。

跨 agent 契约成立：P2 依赖的 `bearer_invalid_transient=1` 由对端落在 `provider.rs:1661`，
P2 核实逐字节一致、源码守卫跑通。

两个 agent **顶回了派单里的错误**（这是好事，说明它们真的独立核实）：
- P5-c 判 `not_reproducible` —— 审查者前提错了，`mark_first_token_if_content` 不只在
  `process_kiro_event` 调，`generate_final_events` 也调
- P6-a 说「结论成立但 brief 给的机制是错的」—— 裸 429 不走我说的那条升档路径

### 主控（我）自己做的四件

1. **接上 P3 的跨文件缺口** `service.rs`：新增 `region_probe_throttled` 分支
   （`AccountThrottled` **刻意不禁用**）+ 并入跳过 `get_usage_limits_for` 的门。
   理由（承重）：`ids_needing_region_probe` 过滤 `!e.disabled` ⇒ 禁用后**连重启回填都不再重探**；
   而不禁用的最坏态是退回 `config.region` 基线，真打错区走 `report_failure → TooManyFailures`
   —— **那个原因在自愈白名单里**。即不禁用严格优于禁用。
2. **加守卫测试** `account_throttled_must_not_disable_credential`，并做了**真回退验证**：
   把 `AccountThrottled` 并进 `region_probe_failed` 的 `matches!` → 测试 FAIL 且报正确断言 → 改回。
3. **全仓扫孤立测试**（P6 发现的那类：`#[test]` 被上一条的文档块吃掉）。扫出 3 处，
   2 处是真孤立测试、**从未运行过**：
   - `service.rs:5344` `multi_open_must_inherit_api_region_from_parent`
   - `provider.rs:2391` `force_refresh_must_skip_api_key_credentials_at_both_sites`
   补属性后**两条一次通过** —— 说明它们守的东西一直对，只是守卫没生效。
   （`http_read.rs:127` 的 `test_client` 是辅助函数，无问题。）
   测试总数 1189 → **1278**。
4. **修 `overview-page.tsx:343`** 的过期注释（「同时通常仅 1-2 个」→ 说明真实上限由
   `pickFireCandidates` 硬顶）。

### 仍需主控接的（agent 无写权限的跨文件项）

- `recovery_metrics.rs`：P1-c + P2 都要计数器。
  ① 额度闸门 `provider.rs:1864` 至今不 bump 任何计数器 → 建议
     `absorb_retry_quota_exhausted` / `bump_absorb_retry_quota_exhausted`
  ② `provider.rs:1905` 与 `:1923` 仍共用 `bump_absorb_budget_exhausted` → 归因混淆
     （运维会去抬 `BudgetSecs`，真正该动的是 `MaxDelaySecs`）
  ③ `IMAGE_MIME_MISMATCH` 建议独立计数器（否则面板 outcome 桶仍是 `bad_request`）
- `token_manager.rs:1538` 的 `is_self_healable_reason` 建议改 `pub(crate)` 并暴露吃 `&str`
  的薄包装 —— P4 现在在 `service.rs:147` 抄了一份列表，有漂移风险（已用穷举 match 守卫兜住）

### WebGL 火焰（独立 agent，已完成并核实）

四条全部落地：实例上限硬顶 4（`pickFireCandidates`）+ 上下文丢失处理 + 去掉
`mixBlendMode`（改 shader 输出预乘 alpha）+ `ResizeObserver` 防抖重建 FBO。
37 测试全绿、`tsc` 全量 0 error、回退验证真做（去掉上限实测 20 个并发上下文）。

它自己抓到一个**修复引入的新缺陷**（我派单没提）：restore 路径上 `isContextLost()` 已回 false，
cleanup 会再调一次 `loseContext()` → 又派发 restored → epoch 又 +1 → **lost/restored 振荡器**，
瞬间烧掉 3 次 restore 配额、canvas 永久空白。已用 `contextWasLost` 标志门住。

⚠️ **只有实例上限那条真被测过**。上下文丢失 / blend / FBO 重建需真实 GPU，标 `[未验]`。
⚠️ 观感有真实差异（alpha 标量 vs `screen` 逐通道），回退是一行。

---

## A. 第一轮（`wf_0413e3e9-002`，已完成）+ 侦察

| Agent | 任务 | 状态 |
|---|---|---|
| A | H2 `AccessDeniedException` 落 502 → 新加独立窄分支 | 跑 |
| B | H3 图片 magic bytes（49 次 400） | 跑 |
| C | M1 thinking 关时只有 reasoning ⇒ 空响应 | 跑 |
| D | H1 CLI 对齐 kiro-rs，`origin: KIRO_CLI` 开关 | 跑 |
| E | M6-1/2 探测改打真实端点 + H4 兜底放行 id 轮转 | 跑 |
| F | M4 吸收层三条 | 跑 |
| G | M6-6 批量清理排除代挂 + M3 额度刷新诊断 | 排队 |
| H | M6-4 通知两 case + M7 区域 UI + M5 SOCKS 编辑 | 排队 |
| 侦察 | IDE 端点 host：`runtime.*.kiro.dev` vs `q.*.amazonaws.com` | 排队 |

+ 5 个对抗审查（A/C/D/E/F/G 各一个，B/H 由主控最后统一验）

---

## B. 🔴 第二轮必须定点修的（我 review 出的派单缺陷，等 agent 产出后一次性改）

### B1 D 的开关粒度错了（我派单时的设计缺陷）
我写的是"配置项默认关"= **全局**开关。但用户要的是"单号开、比 429 率"，
全局开关做不到 —— 一开就是全池。
**正解**：按凭据的字段（照 `endpoint` 的范式：凭据级覆盖 + 全局默认兜底）。
配置项本身留着当全局默认，加凭据级字段是**增量不是重写**。

### B2 D 的字符串 replace 可能静默失效
kiro-rs 用 `body.replace("\"origin\":\"AI_EDITOR\"", ...)` —— 依赖 JSON 序列化**不带空格**。
`serde_json::to_string` 不带空格所以大概率成立，但若某处用 `to_string_pretty`，
替换静默失败、**开关看起来生效实际没变**。
**正解**：解析 JSON 后改字段，或显式验证序列化形状 + 加一条测试钉死。

### B3 E 的"畸形请求拿 400"判据未经实测
我暗示可用畸形请求拿 `400 ValidationException` 当"认证过了"的信号。
但上游**可能先校验参数再校验认证** ⇒ 400 和 403 分不开，探测全废。
**正解**：要么先做一次真实实测确认判据，要么保守用现有方式。审查者要重点抓这条。

### B4 A 与 E 的跨 agent 语义耦合（我派单时没提醒）
`bearer token included in the request is invalid` 这个串：
- 在 `classify_probe_result` 里是 **WrongRegion**（区错了）
- A 要在 `handlers.rs` 给它加分支

若 A 映射成"可重试/换号"，而该号**所有区都错** ⇒ **无限换号**。
A 的分支必须与 E 的"探不出可用区就维持禁用"对齐。

### B5 C 的双 ctx（提了但没强制）
`stream.rs` 有 buffered + streaming 两套 ctx。"有没有产出过正文"的状态要各自维护，
只改一条 ⇒ 另一条仍空响应。

### B6 B 的 webp 需解码 ≥12 字节
`RIFF....WEBP` 的 `WEBP` 在**偏移 8**，只解码前 4 字节判不出 webp。

---

## C. 🔴 已知但没派 agent 的（本轮结束前必须处理）

### C1 IDE 端点 host 矛盾 —— ✅ **已判定：不改 host**（2026-08-06）
kiro-rs IDE → `q.{region}.amazonaws.com/generateAssistantResponse`
KiroStudio IDE → `runtime.{region}.kiro.dev/generateAssistantResponse`
且 KiroStudio 注释断言"旧的 q.* 已停用"—— **那句是错的那一个**：`q.*` 未停用，
它是 **CLI 协议**的 host（2026-08 实测四个 `ksk_` 号在 `q.*` 拿 HTTP 200）。注释已更正。

**判定结论：IDE 端点继续用 `runtime.*`**，理由是**证据不对称**而非「对面已停用」——
`runtime.*` 有 4360 请求 / 99.9% 成功的 **IDE 协议**实测；本仓所有 `q.*` 成功记录
**全是 CLI 协议**，没有一条 IDE 协议路径的实测。要迁移须**先补 IDE 协议在 `q.*` 上的实测**。
完整依据（含 DNS 只解析 2 个区、以及一条**未解决的矛盾**：另一实现称 `runtime.*` 高并发下
25–40% 429 而 `q.*` 为 0，与我们 99.9% 冲突，判定它需要同号同区同并发对打两个 host）
写在 **`src/kiro/endpoint/ide.rs` 头注释**。
线上 18 个号全是 `api_key` 走 CLI ⇒ 不影响；一导入 OAuth/US 号仍走 `runtime.*`（已验证路径）。

### C2 M2 `inboundTargetRpm` / `inboundRpmMax` 打架 —— **有意没派**
要碰线上限流配置，而 CLAUDE.md 记着「容量口径是假的」：
按实测改小会把吞吐掐死一个数量级。**必须先做控制实验**，不适合和别人并发改。
下一步：派一个**纯诊断只读** agent（读 `throttle.rs` + `ws-vps/docs/02-tuning.md`，
出改法提案与预期影响，**不改任何配置**）。

### C3 H1 剩余六项 CLI 差异 —— 本轮只动 `origin`
optout `true/false`、`x-amzn-kiro-agent-mode` 发不发、`amz-sdk-request` `max=1/3`、
UA 形状（kiro-rs 不含 machineId，KiroStudio 嵌了）、`x_amz_user_agent` 是否独立值、
`mcp_url` 尾部 `/mcp`。
D 只在注释里记录。**理由**：一次改七项，失败了不知道是哪项。
`origin` 实测有效后再逐项试。

### C4 freebattle/kiro.rs 的 OpenAI 处理研究 —— **上一轮的悬空债**
上一轮用户让下载 `github.com/freebattle/kiro.rs` 深度研究其 OpenAI 处理，
那个 workflow（`wf_b1faa364-c8c`）用户发了两次 resume 提示 ⇒ **大概率没跑完，产出没被采纳**。
journal 有 109 行但我没读出内容（Read 静默）。
注意：**freebattle ≠ ZyphrZero**，本地 `_study/kiro-rs` 是后者，freebattle 那份**没下载过**。

### C5 G-1 批量清理的前端 UI —— 有意留后
G 只交付后端 + API 契约（因为 admin-ui 归 H 独占，避免冲突）。
UI 接线要等 H 完成后单独做。

### C6 六条已上线修复的线上验证
面板 region 列是否真显示 `eu-central-1`、新号探测是否写对区。
**要等有新号导入才能验**（当前 18 个号全部 `apiRegion=eu-central-1` 且 `region`/`authRegion` 空，
修复前后 host 一致 ⇒ 无变化可观测）。

---

## D. 🟡 需用户拍板（我不能替他决定，别自作主张做掉）

1. **cache 三选一**：停掉假 `cache_read` / 保留+标注（已有 `CACHE_ESTIMATED_HEADER`）/
   移植 kiro-rs `CacheMeter`（400+ 行进热路径）。
   已实测**上游无隐式前缀缓存**（首次 0.95833 vs 后续 0.95514，差 0.3%，n=2795/5152）`[未验，来自 HANDOFF]`
2. **分身主份要不要分节点**：用户说"主凭据没有代理"，而 workflow 被派的是"主份也该分节点"，
   **两个方向相反**，HANDOFF-08-05-NIGHT §4 记为未答
3. **403 永久禁用阈值**：取证结论是**不要上按次数的判据**
   （traces retention 只 ~24h，最可能含真封号的样本压根不在库里）
4. **H1 放开节奏**：`origin` 开关做出来后，单号试 or 全池切

---

## E. 未验收的 workflow 产出（HANDOFF-08-05-NIGHT §4）

1. `clone-group-delete` — 分身页「删除整个账号组」
   ⇒ ✅ **已完整落地**（2026-08-06 晚二次复核，读码确认）。
   `clone-management-card.tsx` 的 `pendingGroupDelete` state + `confirmGroupDelete`
   走 `deleteCredentialsBatch(ids, true)`，含部分失败明细 + 二次确认对话框
   （说明软删可从回收站恢复、节点池不受影响），`clones.group.deleteAll*` 三语文案 20+ 键齐全。
   🔴 **上一条判定「未落地」是错的，错法值得记住**：我 grep 的是 `deleteGroup` /
   `delete_group` / `force: true` —— 而实现用的函数名是 `deleteCredentialsBatch`、
   state 名是 `pendingGroupDelete`。**grep 关键词没命中 ≠ 功能不存在**。
   正确做法是读那个面板组件、找「删整组」这个用户动作对应的代码，而不是猜它会叫什么名字。
   ⇒ 据此误派的重新实现单**已撤销**。
2. `clone-node-picker` — 主份不分节点 / 无法手选 / 选择器不过滤分身
   ⇒ 状态未复核。⚠️ 按 1 的教训：**用「找用户动作对应的代码」而不是「猜符号名」**去核。
3. `thinking-tag-leak` — **已确认落地** `stream.rs`（30 处 `invoke_sniff_buffer`/`MAX_PARTIAL_TAG_BYTES`）
   ⇒ 这条的"已确认"是**grep 出符号才写的**，可信。

验收重点：审查报告有没有指出"必须修"的问题。

> ### ⚠️ 方法论（本节踩过，写下来防再犯）
> **workflow 报告"已完成"不等于代码里有。** 报告是 agent 的自述，不是事实；
> 中途失败的 workflow 也会留下"看起来完成"的 journal 条目。
> ⇒ **验收必须 grep 实际符号**（函数名 / 常量名 / 请求参数名），
> 并且**前后端各查一遍**（只查一侧会把"接口加了但前端没接"当成做完）。
> 符号零命中 = 没落地，不管报告怎么写。上面第 1 条就是这么被抓出来的。

---

## F. 收尾流程（8 个 agent 全部完成后，按顺序做）

1. 读 8 份实现报告 + 5 份审查判决，`verdict=ineffective` 的优先处理
2. 用 `resumeFromRunId: wf_0413e3e9-002` 定点修 B1–B6（已完成 agent 命中缓存不重跑）
3. **等全部改完再跑**：`cargo test --no-default-features` 全套（150–300s）
   + `cargo clippy --no-default-features` + `cd admin-ui && npx tsc --noEmit`
   ⚠️ 中途跑没意义：树在动，结果不代表最终状态
4. 只 fmt 自己改过的文件：`rustfmt --edition 2024 <files>`，**禁止全仓 fmt**
5. git plumbing 临时 index 快照 → 推 `deploy/vps` → Actions → `ssh ws-vps 'kirostudio-update'`
6. 部署后核实：`gateway-status` + 面板 region 列 + journal 无 panic
7. 更新本台账 + `OPEN-ISSUES-2026-08-06.md` 的状态

---

## G. 硬约束（违反造成真实损失，逐条来自实测事故）

1. 工作树有其它会话的未提交改动（78 文件）。禁止对真实 index 做
   `checkout`/`switch`/`stash`/`reset`/`commit`/`add`；**禁止全仓 `cargo fmt`**
2. 提交用 git plumbing 在临时 index 快照，**逐个文件 `add`，不要 `-A`**；
   做完验证分支名与 `git status --porcelain | wc -l` 与开始时一致
3. commit message 用 `-F 文件` 而非 `-m`（反引号会被 shell 展开）
4. **禁止 heredoc 写文件** —— 实测会让整个会话的 Bash **永久静默且转录零记录**
5. 推 `origin` 不推 `public`（`public` = PUBLIC 仓已冻结）
6. 不在 VPS 上编译（4 核会抢死正在服务的 sub2api），走 Actions `deploy-build.yml`
7. 不碰线上配置值，依据在 `ws-vps/docs/02-tuning.md`
8. Rust 必须加 `--no-default-features`
9. macOS：没有 `timeout`（是 `gtimeout`）；`--include='*.rs'` 必须加引号；`find -newermt` 不可用
10. **Bash/Read 会整段静默且转录零记录** —— 本轮已发生 3 次。换工具确认范围、然后**等**，
    不要原地重试同一条命令。它会自愈。

### 本仓「纸面测试」第 8 种形态
> **测了分支内部，没测分支顺序。**
真实事故：改三处、四条测试、三次"回退即 FAILED"全过，**而修复无效** ——
测的是纯函数与分支内部形状，与**分支之间的顺序**无关。

---

## H. 429 的真实归因（防止后续再误判）

近 2h 实测：上游真 429 **7689** 次、全池冷却 699 次、吸收层预算不足 504 次。
monitor `by_cred` 的 `cred=None` 一桶：**12110 请求 / 95.2% rate_limited / 0 成功**
（还没分到号就被挡掉的）。
逐号 `rate_limited_pct` 多在 0–3%（#563 0.5%、#503 0%、#632 0.3%），**无一呈 403 特征**
⇒ 池子 region 全对。**429 是号池容量不够，不是端点错、不是 region 错。**

压 429 只有三条路：**加号** / **降 `inboundTargetRpm`** / **试 H1 的 `origin: KIRO_CLI`**。

---

## I. 回滚

```bash
ssh ws-vps 'kirostudio-update rollback'   # 回到 kirostudio.prev
ssh ws-vps 'gateway-status'               # 全链路巡检
```
当前线上：`97afaf0`（六条 region 修复已上线并验证）
