export const meta = {
  name: 'kirostudio-seven-fix-recon',
  description: 'KiroStudio 七项需求：并发深度侦察 + 对抗验证，产出精确补丁规格',
  phases: [
    { title: 'Recon', detail: '7 路并发深挖：SSRF/回收站/OTA私库/bg缓存/导入契约/调度文案/缓存开关' },
    { title: 'Verify', detail: '对抗验证最高危的三条结论，反驳优先' },
    { title: 'Synthesize', detail: '合并为按文件分组的串行实施顺序' },
  ],
}

const SPEC = {
  type: 'object',
  required: ['title', 'confirmed', 'root_cause', 'evidence', 'patch_plan', 'risks', 'tests'],
  properties: {
    title: { type: 'string' },
    confirmed: { type: 'boolean', description: '缺陷是否被代码证据确证（非推测）' },
    severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low', 'none'] },
    root_cause: { type: 'string' },
    evidence: {
      type: 'array',
      items: {
        type: 'object',
        required: ['file', 'line', 'quote', 'why'],
        properties: {
          file: { type: 'string' },
          line: { type: 'integer' },
          quote: { type: 'string' },
          why: { type: 'string' },
        },
      },
    },
    patch_plan: {
      type: 'array',
      items: {
        type: 'object',
        required: ['file', 'anchor', 'change'],
        properties: {
          file: { type: 'string' },
          anchor: { type: 'string', description: '精确定位串（函数名或唯一代码行），供 Edit 使用' },
          change: { type: 'string', description: '要改成什么，含具体代码/逻辑' },
        },
      },
    },
    risks: { type: 'array', items: { type: 'string' } },
    tests: { type: 'array', items: { type: 'string' }, description: '需新增的回归测试，行为描述式命名' },
    open_questions: { type: 'array', items: { type: 'string' } },
  },
}

const REPO = [
  '仓库 /Users/dwgx/Documents/Project/KiroStudio（Rust/Axum 网关 + React 面板）。',
  '硬约束：',
  '- 只读侦察，**绝对不要修改任何文件**。你的产出是给主会话用的补丁规格。',
  '- 工作树有几十个未提交文件（多会话并行），禁止 git checkout/stash/reset/commit/add，禁止全仓 cargo fmt。',
  '- 测试全在各源文件内联 #[cfg(test)]；src/test.rs 与 src/debug.rs 是孤儿不参与编译。',
  '- 构建一律 --no-default-features（default=native-tls 与出厂配置相反）。',
  '- 注释与文案以中文为主。',
  '每条结论必须引用 file:line 的真实代码，禁止凭印象断言。若查证后发现不是缺陷，把 confirmed 设 false 并说明为什么不是。',
].join('\n')

phase('Recon')

const tasks = [
  {
    label: 'A-SSRF-fakeip',
    prompt: [
      REPO,
      '',
      '【任务 A】custom_api 上号被 SSRF 误杀。',
      '',
      '现象：用户填 base_url = https://api.uu6.top，面板报「添加失败: 自定义 API base_url 校验失败(SSRF 防护)」。',
      '我已实测：api.uu6.top 解析到 198.18.0.46。而 src/common/ssrf.rs 的 is_forbidden_ipv4 把',
      '198.18.0.0/15（RFC 2544 基准测试段）列为禁止段。',
      '',
      '关键背景：198.18.0.0/15 正是 Clash/Surge/Mihomo 等代理软件 fake-IP 模式的默认 fake-IP 池。',
      '开了 fake-IP 的机器上，任何域名都会解析到该段 → 该用户根本无法添加任意 custom_api 中转站。',
      '这与 CLAUDE.md 记录的已知问题 #19 是同一根因（当时只把测试改成 .invalid 域名绕过，生产路径没动）。',
      '',
      '请查证并设计修复：',
      '1. 读 src/common/ssrf.rs 全文。搞清 is_forbidden_ipv4 / is_forbidden_ipv6 / is_forbidden_ip /',
      '   validate_outbound_url / build_guarded_client 的完整调用图。',
      '2. 用 grep 找出所有调用 validate_outbound_url 和 build_guarded_client 的地方，逐一判断该调用点',
      '   面向谁：是管理员在面板里主动填的出站目标（可信度较高），还是匿名/外部可控数据驱动的抓取',
      '   （必须严防）。两类的策略必须不同 —— 这是修复的核心。',
      '3. 设计方案。请评估并给出推荐（不要只列选项，要给结论 + 理由）：',
      '   - 方案甲：新增配置项（如 ssrfAllowBenchmarkRange 或更通用的 ssrfExtraAllowedCidrs），',
      '     默认 false/空，管理员显式放行。注意 src/model/config.rs 的三层热重载约定。',
      '   - 方案乙：区分「管理员显式配置的出站目标」与「外部数据驱动的抓取」两条策略，',
      '     前者放宽 198.18/15（它实际上不是内网、不通向元数据端点），后者维持严格。',
      '   - 方案丙：其他你认为更对的做法。',
      '4. 重点论证：放开 198.18.0.0/15 的真实攻击面是什么？它能不能通向 169.254.169.254 元数据、',
      '   能不能打到内网服务？fake-IP 场景下代理会把该 IP 映射回真实域名再出网，所以网关看到的',
      '   198.18.x 实际并不是一台可达的内网主机 —— 请核实这个推理并给出结论。',
      '5. 错误信息可观测性：当前报错只说「SSRF 防护」，用户完全不知道是自己的代理导致的。',
      '   设计更有指向性的中文报错（要带上实际解析到的 IP + 命中的段名 + 如果是 198.18/15 就提示',
      '   「疑似本机代理 fake-IP 模式，可在设置中放行」）。',
      '',
      '产出精确到 file+anchor+change 的补丁规格，以及需要的回归测试列表。',
    ].join('\n'),
  },
  {
    label: 'B-trash-purge',
    prompt: [
      REPO,
      '',
      '【任务 B】面板「凭据回收站 → 清理」按钮点了没反应（用户原话：一键删除没有呀）。',
      '',
      '我已初步定位，请你完整查证并把链路补全：',
      '- src/admin/service.rs 的 cleanup_trash(older_than_days)：None 时取 config.trash_retention_days',
      '  （默认 30），然后调 token_manager.purge_expired_trash(keep_days)。',
      '- src/kiro/token_manager.rs 的 purge_expired_trash：retention_days == 0 时直接 return 0',
      '  （语义是"永久保留"），非 0 时只删 cutoff 之前的条目。',
      '- admin-ui 的 settings-page.tsx / ops-page.tsx：trash 分区的 supportsDays 为 false，',
      '  所以前端提交 olderThanDays: undefined。',
      '',
      '因此推论：67 条新删除的凭据（都在 30 天内）→ 清理 0 条；而想全清时传 0 又被解释成',
      '"永久保留"直接返回 0 → 没有任何入参能清空回收站。请核实这个推论是否成立，特别是：',
      '1. 读全 purge_expired_trash 与它的所有调用方（后台定时任务 + admin 清理），确认 0 的双重语义。',
      '2. 前端 trash 清理弹框到底提交了什么（读 settings-page.tsx 的 handleConfirm 与 ops-page.tsx 对应处）。',
      '3. 后端返回的 removed=0 前端如何展示 —— 是不是 toast 了一句"清理完成，共移除 0 项"让用户以为坏了。',
      '4. 回收站还有没有别的清空入口（list_trash / purge_credential / purge_trash_batch 路由），',
      '   它们能不能达到一键清空？如果能，那前端为什么没用？读 src/admin/router.rs 与',
      '   admin-ui 里对应的 api 封装。',
      '',
      '设计修复。要点：',
      '- 必须能真正全清，且不能破坏 trash_retention_days=0 表示"后台任务永不自动清"的既有语义',
      '  （这两个 0 的含义冲突是根因，方案要显式解决，比如新增 purge_all_trash 或让 admin 路径',
      '  走一个独立的 force 参数）。',
      '- 前端要给出真实反馈：清了几条、为什么是 0（"回收站里没有超过 N 天的条目，是否全部清空？"）。',
      '- 权限与安全：全清是不可逆操作，需不需要二次确认？现有 TrashConfirm 机制是怎样的？',
      '',
      '产出精确补丁规格 + 回归测试列表。',
    ].join('\n'),
  },
  {
    label: 'C-OTA-private',
    prompt: [
      REPO,
      '',
      '【任务 C】私有仓库 OTA「检查更新 / 一键升级」要做到精细完美。',
      '',
      '读 src/admin/update.rs 全文（645 行）。当前状态：',
      '- github_repo() 读环境变量 KIROSTUDIO_UPDATE_REPO，update_token() 读 KIROSTUDIO_UPDATE_TOKEN。',
      '- 有令牌时 github_api_candidates_for / asset_candidates 只走直连（不把 PAT 交给 gh-proxy 镜像）。',
      '- 资产下载 URL 是 https://github.com/{repo}/releases/download/{tag}/{asset}。',
      '',
      '我怀疑的核心缺陷（请务必严格查证，这条决定整个需求成败）：',
      '对私有仓库，github.com/{owner}/{repo}/releases/download/... 这个浏览器下载地址',
      '不接受 PAT 认证（它依赖 web session cookie），带 Bearer 也会 404。私有仓库拿资产',
      '必须走 Releases API：',
      '  1) GET /repos/{owner}/{repo}/releases/tags/{tag} → 找到 assets[].id',
      '  2) GET /repos/{owner}/{repo}/releases/assets/{id}，带 Accept: application/octet-stream',
      '     → 302 到 objects.githubusercontent.com 的预签名地址',
      '请查证这个判断（可以 WebFetch/WebSearch 查 GitHub 官方文档确认），若成立，说明现在',
      '「配了 token 的私有仓库点一键升级」必然在下载阶段失败 —— token 填对了也没用。',
      '',
      '在此基础上做完整设计：',
      '1. 私有仓库的资产下载改走 Releases API asset endpoint；公开仓库保留现有直链（不回退功能）。',
      '   注意 sha256 校验文件也是一个 asset，同样受此影响，且它必须保持「与二进制独立可信信道」',
      '   的既有安全性质（读代码里 download_from_github_direct 的注释）。',
      '2. 重定向与 Authorization：reqwest 默认跟随 302。GitHub 的预签名对象地址不需要',
      '   Authorization，而带着一个 Bearer 打到 S3 系存储可能被拒（有的会 400）。请确认 reqwest',
      '   跟随重定向时是否会把 Authorization 头带过去（跨 host 时的行为），并设计正确做法。',
      '3. CLAUDE.md 记录的已知问题 #2「OTA 下载无大小限制」仍只修了一半：Content-Length 预检',
      '   在 chunked 响应下不生效，随后 resp.bytes() 仍无界。请设计流式截断读取',
      '   （参考 src/admin_ui/router.rs 里背景图那两处已经这么做的实现）。',
      '4. 令牌可观测性：token 为空/无效/权限不足（fine-grained PAT 少勾 Contents:read）时，',
      '   面板现在只会看到一个笼统失败。设计能区分这几种情形的中文错误：',
      '   401 = 令牌无效/过期；403 = 权限不足或触发限流（看 X-RateLimit-Remaining 区分）；',
      '   404 = 仓库名错或令牌无该仓库权限（私有仓库对无权令牌一律回 404，这点要讲清）。',
      '   绝不能把令牌本身写进日志或 API 响应。',
      '5. 检查 check_for_updates 在私有仓库下的完整路径（tags API + compare API）是否都带了认证、',
      '   有没有哪个漏了 with_update_auth。',
      '6. 面板侧要不要暴露「当前 OTA 目标仓库 / 是否已配置令牌（只报 true/false 不回显值）」',
      '   以便运维自查？读 src/admin/types.rs 里更新相关的响应结构与 admin-ui 的更新卡片。',
      '',
      '产出精确补丁规格 + 回归测试列表（注意：不能在测试里打真实网络，参考 #19 的教训）。',
    ].join('\n'),
  },
  {
    label: 'D-bgcache-clear',
    prompt: [
      REPO,
      '',
      '【任务 D】存储占用面板里「登录背景图缓存 (内存)」无法清理。',
      '',
      '用户看到的面板：',
      '  请求明细 (SQLite)  6.7 MB · 7432 项   [查看] [清理]',
      '  用量日志 (JSONL)   3.7 MB · 2 项      [查看] [清理]',
      '  凭据回收站         30.4 KB · 67 项    [查看] [清理]',
      '  登录背景图缓存 (内存)  内存            ← 没有可用的清理',
      '',
      '请查证到底哪一环断了：',
      '1. 后端：src/admin/service.rs 的 storage_stats 第 4 段 bg_cache 分区（约 2488 行附近）与',
      '   storage_cleanup 的 bg_cache 分支（约 2569 行）。crate::admin_ui::bg_pool_stats() 与',
      '   clear_bg_pool() 在 src/admin_ui/router.rs（约 117 / 131 行）。看起来后端是通的 —— 请确认。',
      '2. 前端：admin-ui/src/components/settings-page.tsx 的 CLEANABLE_KEYS 与 StoragePartitionRow，',
      '   以及 ops-page.tsx 的同名常量。bg_cache 在不在 CLEANABLE_KEYS 里？按钮为什么没渲染出来？',
      '   注意 in_memory=true 的分区可能被某个条件挡住了，也可能是 bytes=0/items=0 时的渲染分支。',
      '3. bg_pool_stats 报的 bytes 是不是真的内存占用？用户截图里 bytes 那一栏是空的（只显示"内存"），',
      '   而其他三行都有数字 —— 查一下 formatBytes(0) 的渲染结果，以及池空时 stats 返回什么。',
      '4. clear_bg_pool 返回的 freed_bytes 被硬编码成 0（service.rs 的 bg_cache 分支）。',
      '   内存池清空实际释放了多少字节是可知的（清之前 bg_pool_stats 就有），这个 0 是不是个缺陷？',
      '5. 池的"代次"机制（BG_POOL 的 generation/epoch，router.rs 约 60 行）与 clear 的交互：',
      '   清空后在途批次会不会把旧图 push 回池，导致"清了又回来"？读那段注释与实现确认。',
      '6. 顺带核对：ops-detail-dialogs.tsx 的 BgCacheDetailDialog（约 1190 行）「查看」能不能用。',
      '',
      '产出精确补丁规格（前后端都要）+ 回归测试列表。',
    ].join('\n'),
  },
  {
    label: 'E-import-audit',
    prompt: [
      REPO,
      '',
      '【任务 E】批量导入 Key 端点的对接兼容性审计。这是已实现但未提交的代码，要逐条验，',
      '不要复述实现说它对，要真去读代码找反例。',
      '',
      '上游对接方的固定请求（改不了）：',
      '  POST /api/import/keys',
      '  Content-Type: application/json',
      '  Authorization: Bearer <导入Token>',
      '  {"items":[{"key":"ksk_xxx","groups":[],"endpoint":null}],"concurrencyLimit":100}',
      '',
      '十条兼容要求，逐条给「通过 / 不通过 / 有条件通过」+ 代码证据 file:line：',
      '1. 路径必须是 /api/import/keys，不能被重定向到带尾斜杠的形态（POST 会被降级或丢体）。',
      '   验：src/main.rs 的 nest("/api", import_alias_app) 与 src/admin/router.rs 的',
      '   create_import_alias_router。axum 0.8 的 nest + route("/import/keys") 组合实际匹配什么路径？',
      '   有没有 NormalizePath 之类的中间件在别处加了重定向？并发的 /api 与 /api/admin 两个 nest',
      '   会不会冲突（后者是前者的前缀，axum 的 matchit 如何处理）？这条要特别小心。',
      '2. 必须读 Authorization: Bearer。验 src/common/auth.rs 的 extract_api_key 与',
      '   admin_auth_middleware。注意 "Bearer " 前缀大小写敏感性、多余空格、Bearer 后跟空 token。',
      '3. items 必须接受数组，元素至少含字符串 key。',
      '4. groups / endpoint 可选，缺失 / 空数组 / null 都要兼容。特别验 "endpoint": null 与',
      '   "groups": [] 会不会报错。',
      '5. concurrencyLimit 可选，缺失时用服务端默认。注意：现在越界（>999）是直接 400，',
      '   而要求 5 说"缺失时使用服务端默认值" —— 越界该 400 还是该夹取？请给出判断并说明',
      '   对接方会不会因此炸（它传 100，在范围内，但要论证边界行为是否合理）。',
      '6. 相同 Key 重复提交要幂等，不能 502。这条最关键：读 src/admin/service.rs 的',
      '   import_one_key → add_credential → classify_add_error（约 2810 行），追到',
      '   src/admin/error.rs 看 InvalidCredential 映射成哪个 HTTP 状态。然后判断：',
      '   重复 Key 时逐条 error 是什么文案、整体 HTTP 状态是几、算 ok:false 还是 ok:true。',
      '   幂等到底该怎么定义 —— 重复提交应当 ok:true（已存在视为成功）还是 ok:false（明确告知已存在）？',
      '   给出推荐并说明对接方会怎么处理。',
      '7. 成功返回 2xx JSON，含 success/total/imported/failed/items[].ok。验 ImportKeysResponse',
      '   （src/admin/types.rs 约 880 行）的字段与 serde camelCase。注意对接方期望 items[].key ——',
      '   我们返回的是脱敏后的 key（mask_import_key）。这会不会让对接方对不上账？',
      '   它提交明文、拿回 ksk_xxxx…xxxx，能否按顺序对应？（响应顺序与请求一致这点要验证）',
      '   这是个真实的契约风险，请给出结论：保持脱敏（安全）还是回显原文（可对账）还是加索引字段。',
      '8. 鉴权失败 401/403、参数错误 400，不能统一转 502。逐个错误路径验状态码。',
      '9/10. 反代与超时：这两条是服务器侧（Caddy）的事，不改代码。但请说明我们这侧的',
      '   隐含要求：导入 N 个 Key 会串行打上游验活（IMPORT_MAX_IN_FLIGHT=4），100 个 Key',
      '   最坏耗时多少？会不会超过反代默认读超时导致 502（那样对接方就看到 502 了，',
      '   而这正是要求 6 和 8 想避免的）。请算出量级并判断是否需要',
      '   （a）加导入总时长上限 （b）改成异步任务 + 轮询 （c）跳过验活直接入池。给出推荐。',
      '',
      '另外独立审一遍安全面：明文 Key 会不会进日志/响应/持久化异常路径；',
      '别名路由是否真的只暴露这一个端点、鉴权是否与 admin 树完全同源、有无绕过。',
      '',
      '产出：逐条判定表 + 需要修的补丁规格 + 回归测试列表。',
    ].join('\n'),
  },
  {
    label: 'F-scheduling-i18n',
    prompt: [
      REPO,
      '',
      '【任务 F】设置页「调度」相关开关的中文说明太单薄，运维看不懂开关到底影响什么。',
      '',
      '背景（来自 CLAUDE.md，这些是线上刻意偏离代码默认值的项，改回默认会造成真实故障）：',
      '- rateLimitEnabled = false：每号最小间隔 1000ms 会在 241ms 处踢开亲和绑定 → 每次换号 →',
      '  prompt cache 全丢；5339 样本实测速率与 429 率相关性仅 +0.09。',
      '- inboundRpmAuto = false：内置 AIMD 是单向棘轮，429 砍半、回升需 20s 静默×N，',
      '  实测每 6.4s 一次 429 → 单调下滑锁死在下限（卡 30 RPM 而号池能跑 216）。',
      '- inboundTargetRpm：由 throttle-autotune.timer 每 2 分钟按可用号数自动调（号数×72×80%）。',
      '- rpmHardGateOverloadWait = false：true 会在整池饱和时背压等待，高并发下请求堆在网关里。',
      '- credentialRpmLimit = 85：替代内置兜底 30；每号有效阈值 = 85 × headroom 85% = 72。',
      '- trustForwardedHeader = false：sub2api 透传白名单里没有 X-Forwarded-For 也不转发它，',
      '  开了拿不到真实用户 IP，而 client_ip 恒为服务器自身地址，按它配 IP 黑名单会一封封掉全部流量。',
      '',
      '任务：',
      '1. 找出设置页里所有调度/限流/亲和/熔断相关的开关与数值项。读',
      '   admin-ui/src/components/settings-page.tsx 和三份 i18n',
      '   （admin-ui/src/i18n/resources/zh.json、en.json、ja.json）。列出每一项的 i18n key 与现有文案。',
      '2. 与后端真实语义对账：逐项去 src/model/config.rs 读该字段的实际默认值与文档注释，',
      '   再去实际读取点（src/kiro/throttle.rs / rate_limiter.rs / scheduling.rs / affinity.rs /',
      '   health.rs / token_manager.rs）确认它真正影响什么。重点找出前端文案与后端实际行为',
      '   不符、或写错默认值的地方 —— 这类文案错误会直接导致运维误操作。',
      '3. 为每一项设计更好的中文说明。标准：',
      '   - 一句话讲「开了会发生什么 / 关了会发生什么」，不是复述字段名；',
      '   - 有实测依据的（上面那些）要把关键数字写进说明，让人不敢乱改；',
      '   - 明确标注默认值，以及「线上刻意偏离默认」的项要给出警示；',
      '   - 相互影响的项（如 inboundRpmAuto 与 inboundTargetRpm、rateLimitEnabled 与亲和）',
      '     要在文案里交叉引用；',
      '   - 长度控制：主文案一行，细节放 tooltip/hint 次级文案。',
      '4. en/ja 也要同步（i18n 三份 key 必须完全对齐，主会话有校验脚本会跑双向 diff）。',
      '   日文英文不必像中文那么详尽，但不能缺 key。',
      '',
      '产出：按 i18n key 组织的完整文案表（zh/en/ja 三列）+ 需要改的 tsx 结构变动（如果要加 hint 行）',
      '+ 你发现的「文案与实际行为不符」清单（这部分优先级最高，单独列出）。',
    ].join('\n'),
  },
  {
    label: 'G-cache-layer',
    prompt: [
      REPO,
      '',
      '【任务 G】缓存（prompt cache）问题收口。',
      '',
      '必读三份配套文档，从结论那份开始：',
      '- docs/CACHE-EXP0-RESULT.md（实测结论 + 交接状态，先读这份）',
      '- docs/CACHE-RFC.md（分层方案 L0/L1/L2/L3、实验矩阵、分叉预案）',
      '- docs/CACHE-RESEARCH.md（生态调研；注意它对 MetadataEvent.tokenUsage 的推断已被 EXP-0 证否）',
      '',
      '已确证的事实：上游 runtime.{region}.kiro.dev 的 metadataEvent 只有 stopReason 一个字段，',
      '不发 tokenUsage/cacheRead*，所以 RFC 的 L2-2（接入上游真值）取消。',
      '',
      '本次要做的是能立刻落地且零上游依赖的部分，请查证并设计：',
      '1. promptCacheEnabled 是死配置（RFC L2-3 记载：零读取点）。请 grep 全仓确认它真的没有',
      '   任何读取点，然后设计把它复活成真开关：它应当控制什么？（cache_breakdown 注入？',
      '   响应里的 cache_read_input_tokens 字段？影子缓存估算本身？）给出明确语义定义。',
      '   关闭时的行为必须是「不注入估算值」而不是「注入 0」—— 这两者对客户端的含义不同，请论证。',
      '2. promptCacheTtlSeconds 当前 3600，RFC 建议改 300 对齐上游 5m。请找到它的实际读取点，',
      '   确认改这个值到底影响什么（shadow cache 的过期？continuationId 恢复窗口？），',
      '   再判断 3600→300 是不是对的、会不会让续传请求的 cache_read 归零。',
      '   顺带核实 v0.7.43 那条「经 continuationId 恢复 shadow cache 估算」的实现现状',
      '   （grep continuationId / shadow），它与 TTL 的关系。',
      '3. cache_breakdown 注入的是估算值（RFC L2-1 建议停止注入）。读',
      '   src/anthropic/handlers.rs 里 cache_breakdown 的产生与注入路径（约 1025 / 1249 / 2099 行',
      '   附近有 record.cache_read_tokens 的赋值，以及 apply_cache_breakdown 约 1241 行）。',
      '   判断：把估算值当真值下发给客户端（CC 会显示"缓存命中 12000 tokens"）是不是在骗用户？',
      '   RFC 说该停，但停了面板缓存统计就恒 0。请给出推荐方案 —— 比如保留内部统计但不下发给客户端，',
      '   或加一个字段标注 estimated。要论证清楚，这是个产品判断不只是技术判断。',
      '4. EXP-1 / EXP-2 到底能不能做：读 RFC 的实验矩阵章节，把两个实验所需的',
      '   前置条件、预算（专用测试凭据、QPS≤0.2、遇 429 停 5min、总量≤350 请求约 70 credits）、',
      '   以及需要临时加的代码补丁（RFC 说 EXP-1 需要一个临时补丁）说清楚。',
      '   判断这两个实验现在该不该做、需要人工提供什么。注意 converter.rs:747 那次',
      '   0.141→0.075（约 47% 折扣）的观测是关键悬案。',
      '5. 顺带：EXP0-RESULT 记的 reasoningContentEvent 被静默丢弃，文档里有明确的',
      '   「为什么现在不做」四条理由。请核实那四条理由现在是否还成立（特别是第 1 条',
      '   "stream.rs 有 469 处 thinking 引用且另一会话刚改过那块，改动窗口不干净" ——',
      '   现在 stream.rs 的改动是否已经收工稳定）。给出「现在做 / 继续不做」的判断。',
      '',
      '产出精确补丁规格（只针对你判定"现在该做"的部分）+ 回归测试列表 + 需要人工决策的问题清单。',
    ].join('\n'),
  },
]

const recon = await parallel(
  tasks.map((t) => () => agent(t.prompt, { schema: SPEC, label: t.label, phase: 'Recon', effort: 'high' }))
)

const ok = recon.filter(Boolean)
log('侦察完成：' + ok.length + '/' + tasks.length + ' 路返回，确证缺陷 ' + ok.filter((r) => r.confirmed).length + ' 条')

phase('Verify')

const critical = [
  { idx: 0, name: 'A-SSRF-fakeip', thrust: '放宽 198.18.0.0/15 是否引入真实可利用的 SSRF' },
  { idx: 2, name: 'C-OTA-private', thrust: '私有仓库 releases/download 直链带 PAT 是否真的不可用' },
  { idx: 4, name: 'E-import-audit', thrust: 'axum 的 /api 与 /api/admin 双 nest 是否真的不冲突、重复 Key 是否真的不会 502' },
]

const verdicts = await parallel(
  critical.map((c) => () => {
    const finding = recon[c.idx]
    if (!finding) return Promise.resolve(null)
    return agent(
      [
        REPO,
        '',
        '你是对抗审查者。下面是另一个 agent 对 ' + c.name + ' 的结论。你的任务是证伪它，不是确认它。',
        '优先寻找：它引用的 file:line 是否真的写着它说的内容；它的推理是否有跳步；',
        '它的补丁方案会不会引入新缺陷或破坏现有测试。',
        '',
        '审查焦点：' + c.thrust,
        '',
        '被审结论（JSON）：',
        JSON.stringify(finding, null, 2),
        '',
        '请逐条核对它的 evidence —— 亲自去读那些 file:line，确认引用属实。',
        '然后审 patch_plan：这个改法会不会破坏 grep 得到的其他调用点？会不会让现有测试失败？',
        '有没有更简单/更安全的改法？',
        '',
        '如果你找不到问题，就明确说结论成立，并补充它遗漏的边界情形（不要为了显得有产出而编造问题）。',
        '如果你发现它错了，把错在哪、正确结论是什么、正确补丁是什么说清楚。',
        '',
        'confirmed 字段填你自己独立验证后的判断，不是转述被审结论。',
      ].join('\n'),
      { schema: SPEC, label: 'verify-' + c.name, phase: 'Verify', effort: 'high' }
    )
  })
)

phase('Synthesize')

const synth = await agent(
  [
    '你是实施规划者。下面是 KiroStudio 七项需求的并发侦察结果，以及对其中三条最高危结论的对抗验证。',
    '',
    '侦察结果：',
    JSON.stringify(ok, null, 2),
    '',
    '对抗验证（针对 A / C / E 三条，以验证结论为准，与侦察冲突时采信验证方并说明）：',
    JSON.stringify(verdicts.filter(Boolean), null, 2),
    '',
    '请产出一份串行实施计划，因为多项需求会改同一批文件，必须避免互相覆盖。已知的文件冲突热点：',
    '- src/admin/service.rs（回收站清理 B + 背景图缓存 D）',
    '- src/model/config.rs（SSRF 放行配置 A + prompt cache 开关 G）',
    '- admin-ui/src/components/settings-page.tsx（B + D + F）',
    '- admin-ui/src/i18n/resources/zh.json、en.json、ja.json（B + D + F + G 都可能加 key）',
    '',
    '要求：',
    '1. 按文件分组，而不是按需求分组。每个文件列出：要改哪些锚点、每处改什么、来自哪个需求。',
    '   同一文件的多处改动排出先后顺序（避免 anchor 因前一次编辑而失效）。',
    '2. 给出整体实施顺序，原则：先后端契约 → 再前端 → 最后 i18n（i18n 是纯追加，冲突最小）。',
    '   纯后端且无冲突的（如 src/admin/update.rs 只有 C 在动）可以最先做。',
    '3. 标注每一步的验证命令（cargo test --no-default-features / tsc --noEmit / vite build）。',
    '4. 明确列出需要用户人工决策的问题（不要替用户决定的：如 import 响应要不要回显明文 key、',
    '   cache_breakdown 要不要停止下发、EXP-1/EXP-2 要不要跑）。每个问题给出你的推荐 + 理由。',
    '5. 列出被判定为"不是缺陷"或"本轮不做"的项，各附一句理由。',
    '',
    'title 填"串行实施计划"，把计划正文放进 root_cause 字段（用 markdown），',
    'patch_plan 按上述按文件分组填写，open_questions 填需要用户决策的问题。',
  ].join('\n'),
  { schema: SPEC, label: 'plan', phase: 'Synthesize', effort: 'high' }
)

return { recon: ok, verdicts: verdicts.filter(Boolean), plan: synth }
