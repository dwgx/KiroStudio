<!-- HISTORICAL-ARCHIVE-MARK -->
> ⚠️ **这是过程记录，不是当前状态。** 当前状态看仓根 `STATUS.md`（唯一真相源）。
>
> 本文件已确证含**过期断言**。历史上多次出现「后来的会话把过期断言当约束」而做出错误决策
> ——最严重的一次：一句无依据的「`q.*` 已停用」注释直接导致了一次错误的架构迁移，
> 改坏 region 探测让 US 号恒 403，上线后才发现并回滚。
>
> 读本文件时：**任何数字（测试数 / 配置值 / 池容量 / 行号）一律现读现验**，
> 结论性断言先按 `STATUS.md` 核一遍。本文件的价值在于**依据与推导过程**，不在于它的结论。

---

# KiroStudio 交接文档 — 2026-08-03

> 给下一个接手的 AI。**先完整读第 0、1、2 节再动任何代码。**
> 本文取代 `HANDOFF-2026-07-31.md` 与 `HANDOFF-NEXT-TASKS.md`（那两份的待办已完成或已被证伪，见第 6 节）。
> 用户会直接和你对话，**他要的是可用的东西，不是分析报告**。

---

## 0. 六条硬约束（违反会造成真实损失）

1. **工作树有其它会话的未提交改动。** 禁止 `git checkout` / `switch` / `stash` / `reset` /
   `commit` / `add`，禁止全仓 `cargo fmt`。备份**只用 `cp` 到 `/tmp`**。
   ⚠️ 本轮我亲眼见到分支被另一个会话从 `fix/macos-support-and-critical-bugs` 切到 `master`
   并提交（`a066fdf`），dirty 从 72 掉到 2。**每次动手前先 `git status` 确认状态。**

2. **提交/推送用 git plumbing 在临时 index 做快照，基树是 `origin/deploy/vps`**：
   ```bash
   export GIT_INDEX_FILE=/tmp/snap.index && rm -f "$GIT_INDEX_FILE"
   git read-tree origin/deploy/vps
   git add -A -- src admin-ui/src deploy Cargo.toml Cargo.lock
   TREE=$(git write-tree)
   git diff-tree -r --stat origin/deploy/vps $TREE     # 先看清要上线什么
   C=$(git commit-tree $TREE -p origin/deploy/vps -m "...")
   git branch -f deploy/vps $C && unset GIT_INDEX_FILE
   git push origin deploy/vps
   ```
   做完必须核对分支名与 `git status --porcelain | wc -l` 与开始时一致。

3. **`origin` = `dwgx/KiroStudio-skiapi`（private，唯一开发仓库）。
   `public` = `dwgx/KiroStudio`（PUBLIC，已冻结）。** 显式写 remote 名时务必确认不是 `public`。

4. **不要在 VPS 上编译**（4 核会抢死正在服务的 sub2api）。走 GitHub Actions。

5. **只用 Edit 工具改 Rust。** 禁止 `sed`/`python` 批量替换。
   （历史事故：sed 破坏大括号、python 替换删掉三个函数整段、正则造成 209 个编译错误。）

6. **每条改动都要有能抓住旧 bug 的回归测试。**
   验证方法：把修复处改回旧行为 → 跑测试 → **必须 FAILED** → 再还原。
   本轮所有改动都这样验过，其中三次因此**抓出我自己的错误**（见第 5 节）。

---

## 1. 线上现状（2026-08-03 22:30）

| 项 | 值 |
|---|---|
| 版本 | 0.7.46，二进制 sha `af64b0b3c50ab97f` = **批4**（`974f887`） |
| 服务 | active，单实例，零 panic |
| shield | `kiro-shield.service` **active**，在 API 路径上（Caddy 有 4 条路由指向 `:8993`） |
| 号池 | 🔴 **只剩 1 个号 `#443`，无分身** |
| 回滚点 | 7 个（`rollback-pre0746b` / `rollback-b1` / `rollback-b2` / `rollback-b3` / `.prev` 等） |

### 🔴 立刻要做的第一件事：批5 已 CI 成功但**没有部署**

```
origin/deploy/vps = 9d86b36   （批5：分身可强制恢复）
生产实际运行     = af64b0b3   （批4）
CI run 30817749621 = completed/success
```

我推了代码、CI 过了，但**没走下载→校验→hotswap 那一步**。照第 4 节的流程补上。

### 工作树里还有 9 个已验证但未推的文件

```
M admin-ui/src/api/credentials.ts        M src/admin/handlers.rs
M admin-ui/src/components/ops-detail-dialogs.tsx   M src/admin/service.rs
M admin-ui/src/components/settings-page.tsx        M src/kiro/token_manager.rs
M admin-ui/src/i18n/resources/{en,ja,zh}.json
```

⚠️ 这些**就是批5 的内容**（我推快照时用的是同一棵树）。`deploy/vps` 已含它们，
所以**不要再推一次**，直接部署 `9d86b36` 即可。但**动手前先 `git diff origin/deploy/vps -- src admin-ui/src`
确认这句仍成立** —— 别的会话可能又改了这些文件。

---

## 2. 本轮已上线的五批（全部零空窗交接、全部带回归测试）

| 批 | commit | 内容 | 状态 |
|---|---|---|---|
| 1 | `dda2d7b` | 错误分类三条 + `retries` 埋点 + 多开 + hotswap 两坑 | 已上线 |
| 2 | `a3c4c63` | `copies` 第 1 份也绕过去重 + 上线守卫脚本 | 已上线 |
| 3 | `464f161` | api_key 不再走 force-refresh | 已上线 |
| 4 | `974f887` | 分身继承父号 region + 自愈指数退避 | **生产在跑这版** |
| 5 | `9d86b36` | 分身可强制恢复 | CI 过了，**未部署** |

质量门：**988 测试全绿 / clippy 0 error / tsc 干净 / 三语 i18n 各 1523 键对齐**。

### 2.1 错误分类三条（同一类缺陷的三个实例）

三者都因为错误串**匹配不上 `map_provider_error` 任何分支**而落末尾兜底 → **502 无 Retry-After**
→ 客户端把它当服务端故障、退避逻辑不启动、原样重发。

| 缺陷 | 实测量级 | 修法 |
|---|---|---|
| 号池真耗尽（`available == 0`） | 15 分钟窗口内 **2082 次**，单个 5 分钟桶峰值 937 | bail 串补 `retry_after_secs=` → 429 |
| **403 账户级临时风控** | 近 2h 占 **22.3% 流量**（1485/6662），突发形态 | 新增 `is_upstream_temporarily_suspended` → 429 + `Retry-After: 20` |
| 失败记录 `retries` 恒为 0 | 所有失败样本无一例外 | `fail_record.retries = attempts_used` + 循环外计数器 |

403 那条的判据**刻意做窄**：只匹配 `temporarily is suspended` / `TEMPORARILY_SUSPENDED`，
**绝不**泛匹配 `AccessDeniedException` 或裸 403 —— 后者会把真永久封号也吞成可重试，
让客户端对死号无限退避并掩盖真实故障。

`Retry-After: 20` 与 `cooldown.rs` 的 `CooldownReason::SuspiciousActivity`（20s）同源，
不是拍的数字。

### 2.2 api_key 号被三倍加速烧号

```
凭据 #422 刷新瞬态错误（第 1/3），1s 后重试: API Key 凭据不支持刷新 Token
凭据 #422 token 强制刷新失败，计入失败
```

api_key 号没有 refreshToken，`refresh_token()` 对它是契约级 bail（那个 bail 本是给面板
「强制刷新」按钮设计的）。但请求热路径的两处 force-refresh 也会调它 →
错误串不含任何 HTTP 码 → 被刷新层的**黑名单式**瞬态判据当可重试 → 退避重试 3 次（白等 1s+2s）
→ 每轮计一次失败 → 3 次即判死号。**死亡速度放大三倍。**

修法两层：① `provider.rs` 两处调用点加 `!ctx.credentials.is_api_key_credential()`；
② 刷新层瞬态判据排除该错误串。回退验证复现耗时 **3.004s**，修后 **0.00s**，线上验证该日志 0 行。

### 2.3 分身丢 `apiRegion` → 100% 403

多开请求只带 `authMethod` + `kiroApiKey` + `copies`，`api_region` 为 None。
而 CLI 端点 host 是 `q.{api_region}.amazonaws.com`（`endpoint/cli.rs`），拿不到就回退
config 默认 us-east-1 —— 但 **`ksk_` token 按 region 授权** → 403
`AccessDeniedException: The bearer token included in the request is invalid.`

| | 分身成功率 |
|---|---|
| 不继承 region | **0%**（100% auth_failed） |
| 继承后 | **83% / 45% / 100% / 88%** |

⚠️ **我曾把这个误判成「这个 key 不支持分身」**，是用户质疑后才查出真因。教训见第 5 节。

修法：新增 `find_credential_by_api_key`（按 key 的 SHA256 找池中同号），
`copies` 显式给值时继承 `region` / `auth_region` / `api_region` / `subscription_title`。
**刻意不继承** `machine_id`（分身的核心是独立指纹）与 `proxy_*`（每份配不同出口 IP）。

### 2.4 全池自愈无退避 → 加深封禁

用户原话：「**他们已经 403 封号了，不知道为什么一直被自动开启**」。

自愈此前**无任何限频**：只要选不出号且有可自愈的禁用号就立刻复活全池。
实测 **41 分钟触发 36 次**（约每 68 秒）。而 403 `temporarily is suspended` 是上游刚下的惩罚，
每次复活都立刻再打一轮 → 持续撞同一面墙。线上 403 窗口约 10 分钟 → 一个窗口内重试 9 遍。

修法：指数退避 `SELF_HEAL_BASE_BACKOFF=60s` 起翻倍、`SELF_HEAL_MAX_BACKOFF=900s`
（与真实窗口同量级），**任一号 `report_success` 即清零 streak**。
没有清零那条它就是单向棘轮，会涨到 15 分钟再也不回落（与 `health.rs` 那批缺陷同型）。

**线上验证生效**：10 分钟只触发 1 次（修复前 41 分钟 36 次）。

### 2.5 分身无法从回收站恢复

恢复的去重判据只看 key，而**分身与主凭据必然同 key** → 删掉的分身永远恢复不了，
主凭据也一样（只要还有一个分身在池里）。面板反复弹「凭据已存在（kiroApiKey 重复），无法恢复」。

⚠️ **我第一版把判据改成「key + machineId」，被既有测试
`test_restore_duplicate_refresh_token_rejected` 抓住并证否**：入池时 machineId 撞车会
**自动轮换**（同 key 派生出同一指纹 → 第二个换成随机值），所以同 key 的凭据在池里
machineId **永远不同** → 那个判据永不命中 → 等于把护栏整个拆掉。

正解：加 `force` 参数（与 `delete_credentials_batch` 的 force 同款）。默认 false 仍按 key 去重；
分身恢复显式声明意图。**恢复后仍是禁用态**，故 force 不会跳过运维确认直接投入调度 ——
这是它安全的前提。前端两处 UI 在命中「重复」错误时自动重试一次强制恢复。

### 2.6 shield 的 accept 队列只有 5（本轮修的，改在 VPS 上）

`/opt/kirostudio/bin/kiro_shield.py` 的 `Server` 类没设 `request_queue_size`，
Python `socketserver` 默认 **5**。300 并发压测：

| | 修前 | 修后 |
|---|---|---|
| 成功率 | 61.3% | **100.0% (300/300)** |
| 连接被 RST | **116** | **0** |

改成 1024（`somaxconn` 是 32768，够用）。**备份在 `/opt/kirostudio/bin/kiro_shield.py.bak-backlog`**。

⚠️ **这个改动只在 VPS 上，不在本仓** —— shield 不是本仓文件。若重装/重建 VPS 会丢。

---

## 3. 实测数据（设计时直接引用，不要重新测）

### 300 并发压测（修 shield backlog 后）

| 路径 | 成功率 | p50 | p90 | 墙钟 | 备注 |
|---|---|---|---|---|---|
| 经 shield | **100%** | 73.2s | 102.6s | 134.6s | 全部被服务 |
| 直连 KiroStudio | 72% | 36.7s | 58.4s | 80.8s | 84 个 429，零 RST |

**KiroStudio 自己能扛 300 并发**（backlog 1024，零 RST）。有效吞吐 ≈ **134 RPM**
= 1 个账号经 5 份分身的真实容量。

### 429 的真因（我前两次归因都错了，这组数据是对的）

| | 凭据数 | 账号总 RPM | 429 率 | 成功 RPM |
|---|---|---|---|---|
| 单号时期 | 1 | 35.6 | **0%** | 35.6 |
| 5 分身（同账号） | 5 | **56.8** | 12.3% | **49.1** |

「以前一个令牌不 429」是因为那时总吞吐只有 35 RPM，**在账号的干净上限之内**。
分身把吞吐推到 57 RPM，**超过那个上限**才出现 429。账号天花板在 **35~57 RPM 之间**。

净效果是**赚的**：成功吞吐 +38%，代价的 429 被 shield 吸收了 92%（241/263）。

⚠️ **我先前说「网关放行 5×167=835 RPM 所以更早撞 429」是错的** ——
实测每份只有 9.5~14.1 RPM，离 167 阈值远得很，网关根本没放行到那个量。

### 分身实测分流（300 并发期间，5 个凭据）

```
#438  80 请求 88%    #439  99 请求 88%    #440  93 请求 71%
#441 116 请求 78%    #442  85 请求 89%
```

5/5 全部存活、零自愈触发、零 panic。**分身机制是有效的。**

### 烧号速率

6 小时内凭据 id 从 #406 走到 #418，单号存活 **25~60 分钟**、服务 950~2200 请求后被换。
#442 是被 **29 次 403 + 41 次 429** 累积打到 `consecutive_suspicious` 阈值禁用的。

**号池无冗余是所有问题的放大器**，补真号比任何代码改动收益都高。

---

## 4. 部署流程（本轮跑通 5 次，照做即可）

```bash
# 1. 快照推送（见硬约束 2）→ 触发 CI
gh workflow run deploy-build.yml --repo dwgx/KiroStudio-skiapi --ref deploy/vps -f run_tests=true

# 2. 等 completed/success，下载 + 三处 sha256 校验
gh run download <RUN_ID> --repo dwgx/KiroStudio-skiapi -D /tmp/dl
BIN=/tmp/dl/kirostudio-linux-x86_64/kirostudio-linux-x86_64
shasum -a 256 "$BIN"                                   # 本地
awk '{print $1}' /tmp/dl/kirostudio-linux-x86_64/*.sha256   # CI
scp "$BIN" ws-vps:/tmp/ks.new && ssh ws-vps 'sha256sum /tmp/ks.new'   # 服务器
# 三处必须一致，不一致立刻中止

# 3. 建持久回滚点（kirostudio-update rollback 用 mv 会吃掉 .prev）
ssh ws-vps 'cp -a /opt/kirostudio/bin/kirostudio /opt/kirostudio/bin/kirostudio.rollback-<tag>'

# 4. 先 check（起裸实例验健康但不交接，失败则旧实例完全不受影响）
ssh ws-vps 'chmod +x /tmp/ks.new && /tmp/hotswap.sh /tmp/ks.new check'

# 5. 真交接（零空窗）
ssh ws-vps '/tmp/hotswap.sh /tmp/ks.new'
```

### 验证（缺一不可）

```bash
# ⚠️ 用 /proc/*/exe 数实例，不要用 pgrep —— 它会匹配到你自己的 ssh 命令串（本轮踩过两次）
ssh ws-vps 'ls -l /proc/*/exe 2>/dev/null | grep -c "kirostudio$"'   # 应为 1
ssh ws-vps '/opt/kirostudio/bin/kirostudio --version'
# 真实流量：hotswap 的健康检查只打 /v1/models，不碰号池不碰上游，选号/流式的回归它抓不到
# panic 计数、traces 近 5 分钟 outcome 分布
```

### 自动回退守卫

```bash
scp deploy/deploy-watchdog.sh ws-vps:/tmp/
# 先测部署前的**网关侧**基线（剔除 auth_failed/rate_limited/bad_request）
ssh ws-vps 'setsid nohup /tmp/deploy-watchdog.sh <回滚点绝对路径> 20 10 <基线%> > /tmp/wd.log 2>&1 &'
```

判据：panic 零容忍 / 进程与端口存活 / 网关侧成功率**连续 3 轮**跌破容忍。

⚠️ **两条判据是踩坑改出来的，不要改回去**：
- **分母必须剔除 `auth_failed` / `rate_limited` / `bad_request`** ——
  它们分别是上游风控、上游限速、客户端错误，都不由网关版本负责。
  同一窗口两种口径实测差 16.4pp（网关侧 91.4% vs 总成功率 75.0%）。
- **必须连续 3 轮才回滚** —— 第一版单轮触发，19 轮 92% 正常、第 20 轮 58% 就回滚了。
  事后查那 21 个失败是 14 个 `bad_request` + 7 个 `server_error`，而被回滚那版
  **只改了 `copies` 去重语义、根本不碰请求路径**。改进后在生产上验证过：
  ```
  [2/25] ⚠️ 成功率 77% 差 14pp —— 连续劣化 1/3
  [3/25] 成功率 95% 已回到容忍内，连续劣化计数清零
  ```
  旧版本在第 2 轮就会误回滚。

---

## 5. 我犯的错（这一节最值得读）

1. **「这个 key 不支持分身」是错的。** 分身全 403 时我断言是 key 绑定了原机器码，
   并声称"排除了代理"—— 但那是基于旧数据（那批在设代理前就失败过），
   而新那批是**先绑代理才启用的，我从没测过它们直连**。
   用户质疑后才查出真因是**我自己的多开实现丢了 `apiRegion`**。
   **教训：声称"排除了某变量"之前，确认你真的单独测过它。**

2. **429 归因错了两次。** 先说"key 不支持分身"，再说"网关放行 835 RPM 导致更早撞 429"。
   第二个也错 —— 实测每份只有 9.5~14.1 RPM，离 167 阈值远得很。
   **两次都是机制推理讲得通但没拿数据回测。** 第三次先量了单号/多开两个时期的
   账号总 RPM 才得出正确结论。

3. **恢复去重改成「key + machineId」是错的**，被既有测试抓住。
   我没想到入池时 machineId 撞车会自动轮换 → 同 key 的凭据 machineId 永远不同
   → 那个判据永不命中。**教训：改判据前先确认判据的输入在生产里长什么样。**

4. **守卫第一版按总成功率判定，会误回滚**（真触发了一次）。
   **教训：任何"劣化即回滚"的判据都必须先剔除不由被测对象负责的失败来源。**

5. **源码级守卫测试的字面量会自匹配。** 第一版 needle 写成完整字面量，
   它自己也出现在 `include_str!` 读到的内容里 → 多算一处 → **测试在回退前就 FAIL**。
   正解是运行时拼接 needle（`format!("{}{}", "前半", "后半")`）。

6. **`pgrep -cf <路径>` 会匹配到自己的 ssh 命令串**，本轮两次误报"有孤儿实例"。
   用 `ls -l /proc/*/exe | grep -c "kirostudio$"`。

7. **测量 trash.json 时读错了嵌套层级**，误报"302 条全丢原因"。
   实际 87 条有原因（结构是 `{credentials:{...}, deletedAt, ...}`，字段在里层）。

8. **把 sub2api 的 503 算到 KiroStudio 头上。** `/var/log/caddy/access.log` 是
   **多个 site block 共享**的，我只按 URI 过滤没按 Host 分组。
   按 Host 分后：`api.skiapi.dev`（sub2api）2500 个 503，`k1ro.skiapi.dev`（KiroStudio）**0 个**。

9. **给 agent 的 scope 太宽会烧 token 反复失败。** 第一个 workflow 的 config 那路
   塞了 5 件深活 → 833k token / retry 4 次 / 45 分钟仍未完成。
   拆成窄 scope + 输出行数封顶后，11 路全部正常完成。

10. **我说过一次"workflow 还在跑"，其实我已经把它停了。** 状态类陈述必须查证再说。

---

## 6. 待办（按优先级，附我的判断）

### 6.1 🔴 立刻：部署批5 + 补号

批5（`9d86b36`）CI 已成功但未部署，照第 4 节流程走。
号池只剩 1 个号且无分身 —— **补号比任何代码改动收益都高**。

### 6.2 🔴 用户明确要求的六件事（规格已出但**不能直接实施**）

我派了 11 路 agent 出规格，另有 2 路（吸收层）仍未回。
**对抗审查抓出 9 个 BLOCKER，其中 4 个致命** —— 按现有规格实施会打崩生产。

**规格与审查的完整原文已存进仓库**：`docs/clone-mgmt-specs-2026-08-03/`
（11 份 + 一个 `README.md` 说明阅读顺序）。**先读 `REVIEW-adversary-9-BLOCKERS.md`。**

原始 journal 在
`~/.claude/projects/-Users-dwgx-Documents-Project-KiroStudio/85cd385d-.../subagents/workflows/wf_a58638ab-22d/journal.jsonl`
（若要重新提取或看 agent 的完整返回值）。

用户的六件事：
1. **shield 内置进 KiroStudio + 设置里能开关**
2. 分身凭据要有**标签（tag）**
3. 设置里新增**「分身管理」页**：加 SOCKS 节点 / 看主凭据 / 主凭据可查余额 /
   一键按主令牌生成分身 / 一键删除分身 / socks 管理**沿用现有测速容器**
4. **推号（`import/keys`）内置 + 设置开关 + 可选自动分身**
   ⚠️ **自动分身默认必须关闭** —— 用户原话「更新之后不要直接开启，
   不然协议不一样不能自动上号了」
5. 同账号**剩余用量要同步显示**（截图确证：同一个 key 显示 64.7/66.1/90.1/66.5/90.2%）
6. 「一次性完美交付」—— 前后端 + 三语 i18n 都要齐

### 6.3 9 个 BLOCKER（实施前必须解决）

**致命：**

1. **吸收层重入 `acquire_admission`。** 它在 `call_api_with_retry` 内、failover 循环外
   （`provider.rs:810`），"每个客户端请求只过一次"这个不变量靠"一请求 = 一次
   `call_api_stream`"成立。吸收层重打整个 `call_api_stream` 就打破它：
   单请求吃 **5 个入站令牌**、最多 5×12 = **60 次上游调用**；
   300 并发 × 5 轮 = **1500 次准入/分钟 vs 288 个令牌** → 桶恒空。
   **修法**：重试下沉到准入闸门之下，或传 `already_admitted` 标志。

2. **入站准入超时被误判为可吸收。** 它的错误串是
   `入站限速排队超时(...)retry_after_secs=N`，与全池冷却**共用同一个标记、字符串上不可区分**
   → 吸收层会**重试网关自己的背压信号**（2 轮 × 30s = 客户端等 60s 才拿到 429，
   今天是 <2s）。**修法**：准入超时换独立标记（如 `inbound_admission_timeout=1`），
   分类器显式返回不可吸收。

3. **前端依赖的字段在后端无来源。** 规格说改 `CredentialSnapshot` —— **仓里没有这个类型**。
   面板凭据列表实际收的是 `CredentialStatusItem`（`types.rs:24`，构造在 `service.rs:384`），
   后端规格完全没碰它。

4. **推号 + 自动分身整节缺失。** 而且 `import_keys` 走**手写解析器**
   （`handlers.rs:427-439` 的 `parse_import_keys_request`，注释说明刻意不用 derive），
   serde 默认值帮不上忙，必须手写 `unwrap_or(false)` 并用测试钉死。
   `/api/import/keys`（`router.rs:177`）是外部 kiro-accounting 入口，
   **那条路径永远不会带 `autoClone` 字段** —— 若默认取 true，外部推号会静默给每个新号造 16 份分身。

**高：**

5. **`refresh_all_balances_gently`（`service.rs:901-940`）对每个未禁用 id 直接调
   `fetch_balance`，完全绕过 `balance_cache`** → 5 个分身每 30 分钟仍打 5 次 `web_portal`。
   "减少上游探测"这个主要收益是零。
6. **`get_cached_balances`（`service.rs:865-880`）在返回前叠加按 id 的乐观修正** →
   扇出同一份真值后每个 id 再各自加自己的 delta → **又变成 5 个不同百分比**。
   即**用户报的那个 bug 按规格改根本没修掉**。必须按账号聚合。
7. **`cred:{parent_id}` 遇 id 复用会把无关新号并进旧组。**
   重启时 `next_id = max_existing_id + 1`，若父号恰是最大 id、被删且回收站清空 → id 被重新发放。
8. **key 轮换使账号组键分裂**（`add_credential` 会覆写 `kiro_api_key`）。
9. **吸收层预算判据**必须按"一轮最坏耗时（45s + 20s）"检查，而非 `min_delay`，
   否则第 2 轮必被 deadline 半路砍断、白打一轮上游。

### 6.4 我的建议

审查那份质量很高（判据具体、有 file:line、区分了"防守成立"与"必须修"），
而三份设计规格有实质缺口（还被我传参时 `.slice()` 截断了）。

**建议：不要再派一轮 agent 出规格，直接按审查意见自己写并实施。**
审查已经把该改什么说得足够具体。若要派 agent，务必窄 scope + 输出封顶
（见第 5 节第 9 条的教训）。

实施顺序建议：先做**余额同账号共享**（用户能立刻看到、且不碰热路径），
再做**分身 tag + 分身管理页**（纯新增），最后才做**shield 内置化**
（它碰请求热路径且有 BLOCKER 1/2/9，风险最高）。

### 6.5 已明确判定不做的（有证据，别重做）

- `p_avail` 批量化 —— 无实测支撑 + 碰选号热路径 + 行为不可观测故无法测试保护
- SQLite `busy_timeout` —— rusqlite 已默认 5000ms 且 `TraceDb` 单连接单 Mutex，压根不争锁
- `spawn_blocking` 包 trace_db 查询 —— 真正的争抢在那把 `parking_lot::Mutex` 上
- 上游 `hank9999/kiro.rs` 逐条对照 —— 28 条 fix 零可修项

### 6.6 需要用户拍板的

| # | 决策点 | 我的判断 |
|---|---|---|
| D1 | `ccAutoBuffer` 线上 `true` vs 两份旧文档记 `false` | **两条实测证据链互相矛盾**，改任一侧都可能真回归。需用户定 |
| D2 | 关机路径 8s 是"地板"不是"上限" | 见 6.7，改法明确但碰生产停机路径 |
| D3 | 账号真实容量 35~57 RPM，是否要按账号（而非按凭据）限流 | 碰选号热路径，建议先加默认关闭的开关 |

### 6.7 关机路径的假注释（已定性，未修）

```
16:12:52  收到停机信号，开始 drain
16:13:00  drain 宽限期结束（睡满 8s）
16:13:02  systemd SIGKILL      ← TimeoutStopSec=10 到期
```

`shutdown_with_drain_cap()` 是「等信号 → `sleep(8s)` → 返回」。而
`with_graceful_shutdown` 的 future 完成才**开始**停止接新连接，之后 `.await` 无上限。
所以：那 8 秒里服务**还在接新连接**；8 秒后才开始真 drain，而 systemd 只剩 2s。

**24 小时 42 次停机，`服务已优雅停机` 出现 0 次、SIGKILL 42 次 —— 优雅停机从未成功过。**
`SHUTDOWN_DRAIN_CAP_SECS` 这个名字什么都没封顶，`main.rs:702-703`/`714` 承诺的
"未 drain 完的连接被断开"**代码里不存在**。

修法：信号到就让 future 立刻返回，用 `select!` 让 serve 与 8s 竞速。
⚠️ `main.rs` 无 test 模块，这条无法用内联单测覆盖。

---

## 7. 环境与工具

```bash
# ⚠️ 必须先构建前端：rust-embed 是编译期嵌入 admin-ui/dist，缺 dist 时 cargo 报 E0599
cd admin-ui && pnpm install --frozen-lockfile && pnpm build && cd ..

# ⚠️ 一律加 --no-default-features（Cargo.toml 的 default=["native-tls"] 与出厂配置相反）
cargo test --no-default-features --bin kirostudio      # 当前 988 全绿
cargo clippy --no-default-features --bin kirostudio    # 0 error
cd admin-ui && npx tsc --noEmit                        # 干净
```

⚠️ **测试里用 reqwest 打 127.0.0.1 必须 `.no_proxy()`**（本 crate 开了 `system-proxy` feature）。
⚠️ **测试不得依赖真实 DNS/网络**。`add_credential` 会调 `get_usage_limits_for`（真实上游往返），
所以穿它的行为测试写不了 —— 用**源码级守卫测试**（本仓已有惯例，见
`provider.rs` 的 `should_emit_usage_record_in_mcp_success_branch`）。

### SOCKS 代理节点（用户提供，已实测全部可用）

5 个节点，各自独立出口 IP，端口 40002，用户名/密码认证，UDP 开启。
凭据设代理走 `POST /credentials/{id}/proxy`，⚠️ **请求体是 snake_case**
（`proxy_url` / `proxy_username` / `proxy_password`）——
`SetProxyRequest`（`handlers.rs:311`）**没有** `rename_all="camelCase"`，
这是本仓少数例外。发 camelCase 会被当字段缺失 → `Option` 默认 None → **语义变成"清除代理"、
返回 200 但什么都没设**（我踩过这个坑）。

节点明细在用户对话里（含明文密码，故不写进本文档）。

### 线上只读排查

```bash
ssh ws-vps 'gateway-status'          # ⚠️ 会假绿：只看"是否在冷却中"不看"拿去用能否成功"
# 判断号池真实健康要看 traces.db 的逐号成功率
ssh ws-vps 'sqlite3 -header -column /opt/kirostudio/data/usage/traces.db "
  select credential_id, count(*) n, round(100.0*sum(outcome=\"success\")/count(*),1) pct
  from traces where ts_ms>(strftime(\"%s\",\"now\")-1800)*1000 group by 1;"'
```

### fan-out subagent 的注意事项

agent 调用走的正是 `k1ro.skiapi.dev`（我们自己的网关）。
本轮 `spec`/`attack` 两路就是被**待上线的那个 502 缺陷**打死的
（报错原文 `API Error: 502 上游 API 调用失败（未识别错误）`）。
号池紧张时高并发必然打穿它。**建议 agent 只做只读侦察，写代码由主线串行做。**

---

## 8. 线上配置（刻意偏离默认值，不要"按默认更安全"改回去）

| 配置 | 线上值 | 为什么 |
|---|---|---|
| `inboundThrottleEnabled` | **true** | 不是误改 —— `throttle-autotune.timer` 每 2 分钟在管 `inboundTargetRpm` |
| `inboundTargetRpm` | 133 | 由 autotune 按可用号数算。⚠️ 它基于**虚高的容量 167** |
| `inboundRpmAuto` | false | 内置 AIMD 是单向棘轮，实测卡在 30 RPM 而号池能跑 216 |
| `rateLimitEnabled` | false | 每号最小间隔 1000ms 会在 241ms 处踢开亲和绑定 → prompt cache 全丢 |
| `cooldownEnabled` | false | 死号自动禁用已不依赖它 |
| `credentialRpmLimit` | 200 | ⚠️ **与实测严重不符**：账号真实干净容量 35~57 RPM。见 6.6 D3 |
| `rpmHeadroomFactor` / `rpmReserveSlots` | 85 / 3 | `200×85%−3 = 167` = 每凭据饱和阈值 |
| `ccAutoBuffer` | true | ⚠️ 与旧文档矛盾，见 6.6 D1 |
| `trustForwardedHeader` | false | sub2api 不转发 XFF，开了也拿不到真实 IP，按它配黑名单会**一封封掉全部流量** |

⚠️ **`throttle-autotune`（VPS 上的 Python 定时器）把 Rust 的容量算式重写了一遍**，
两份实现都只读配置、零实测输入 → 它算出的"池容量"是把人填的 200 乘两下。
它在 `~/Documents/WorkSpace/ws-vps/config/usr/local/bin/throttle-autotune`。

---

## 9. 一句话总结

**代码侧本轮修了 5 批共 10 条缺陷，全部带回归测试、全部零空窗上线（批5 待部署）。
运维侧最大约束是号池只剩 1 个号。用户要的六件事规格已出但有 9 个 BLOCKER，
建议按对抗审查的意见自己写而不是再派 agent。**

**用户想要的是能用的东西。他明确抱怨过"不做没有证据支撑的改动"，
也明确要求过"不能非常非常保守"——他是大型中转，吞吐优先。**
