<!-- HISTORICAL-ARCHIVE-MARK -->
> ⚠️ **这是过程记录，不是当前状态。** 当前状态看仓根 `STATUS.md`（唯一真相源）。
>
> 本文件已确证含**过期断言**。历史上多次出现「后来的会话把过期断言当约束」而做出错误决策
> ——最严重的一次：一句无依据的「`q.*` 已停用」注释直接导致了一次错误的架构迁移，
> 改坏 region 探测让 US 号恒 403，上线后才发现并回滚。
>
> 读本文件时：**任何数字（测试数 / 配置值 / 池容量 / 行号）一律现读现验**，
> 结论性断言先按 `STATUS.md` 核一遍。本文件的价值在于**依据与推导过程**，不在于它的结论。

---

# KiroStudio 交接 — 2026-08-05 05:00

> 给下一个接手的 AI。**先读第 0、1、2 节再动任何代码或配置。**
> 本文接续 `HANDOFF-2026-08-04-NIGHT.md` 与 `STATUS-2026-08-05.md`。
> 用户是大型中转，吞吐优先，要能用的东西而不是分析报告。

---

## 0. 七条硬约束（违反会造成真实损失）

1. **工作树有其它会话的未提交改动**（本轮全程 50~51 个文件 dirty）。禁止
   `git checkout` / `switch` / `stash` / `reset` / `commit` / `add`（对真实 index），
   禁止全仓 `cargo fmt`。

2. **提交/推送用 git plumbing 在临时 index 做快照，且必须按文件 `add`，不要用 `-A`**：
   ```bash
   export GIT_INDEX_FILE=/tmp/snap.index && rm -f "$GIT_INDEX_FILE"
   git read-tree origin/deploy/vps
   git add -- src/kiro/token_manager.rs src/admin/service.rs   # ← 逐个列，不要 -A
   TREE=$(git write-tree)
   git diff-tree -r --stat origin/deploy/vps $TREE             # 先看清要上线什么
   C=$(git commit-tree $TREE -p origin/deploy/vps -F /tmp/msg.txt)
   git branch -f deploy/vps $C && unset GIT_INDEX_FILE
   git push origin deploy/vps
   ```
   ⚠️ **`-A` 会把另一个会话的半成品一起推上线。** 本轮全程有人在改
   `src/anthropic/stream.rs`（thinking tag 泄漏修复，205 行未验证），
   我每次都按文件 add 才没把它带上去。**接手后先确认它归谁。**

3. **commit message 用 `-F 文件` 而不是 `-m "..."`** —— 反引号会被 shell 展开，
   本轮踩过一次（三个术语被吃掉，只能 force-push 修）。

4. **只用 Edit 工具改 Rust。** 禁止 sed/perl/python 批量替换多行源码
   （本轮我用 python 改过 import 与移动结构体，那是不得已，且每次都立刻编译验证）。

5. **每条改动都要有能抓住旧 bug 的回归测试 + 「回退即 FAILED」验证。**
   本轮这个流程抓出我**四个纸面测试**（见第 6 节）。没做过验证的不要说做了。

6. **不要在 VPS 上编译**（4 核会抢死正在服务的 sub2api）。走 GitHub Actions。

7. **subagent 在这个项目里目前是坏的。** 本轮 4 次尝试、**18 个 agent 全部启动即死**
   （journal 里只有 `started`、0 个 `result`，failure 是 `no progress for 180000ms`，
   0 token / 0 tool call）。交接文档早先警告过「agent 走的正是生产网关」，
   但这次连一个字节都没写，不是被网关拖死那么简单。**不要指望它们，串行做。**

---

## 1. 线上现状（2026-08-05 04:52 实测）

| 项 | 值 |
|---|---|
| 二进制 | `30d39fb3be90eebe`（= `f1abb07`），启动于 04:45:38 |
| 服务 | active，**单实例**，0 panic |
| 号池 | **4 个全部启用**：#530(ok=1557) / #532(262) / #533(179) / #534(129)，全部 `endpoint=cli` |
| 近 10 分钟 | 20~55 req/min，**~100% 成功，0 个 429** |
| 近 1 小时 | 54.0% 成功 / 45.0% rate_limited ← 那 45% 来自池子只有 1 个号的时段 |
| shield | 224 resolved / 733 attempts / **放大 3.27x** / **gave_up 0** |
| 今日禁用 | 13 次 |

### ⚠️ 四个号**全部** `apiRegion=None`

```
#530 #532 #533 #534   apiRegion=None   →  全靠 config.region=us-east-1 回退
```

**改 `config.region` 会让这四个号同时 100% 403。** 这不是降级，是全废。
`region_probe` 只在「上号时」探（`add_credential` 里），这四个号是在探测接线之前入池的，
或者推号方给了 region 字段（现在没有）。**接手后建议逐个探一遍再写死**，
现在有端点可以做（见 2.6）。

### 配置现状

| 配置 | 值 | 说明 |
|---|---|---|
| `cooldownEnabled` | **true** | 我 04:0x 开的（此前 false，让三条已上线修复空转） |
| `cooldownScalePct` | 40 | 15s 基线 × 40% = 6s，被 `scale_floor` 抬到 **8s** |
| `autoDisableSuspicious` | **true** | 我开的。⚠️ 必须与 cooldown 同开，见 2.2 |
| `allCoolingFastFail` | false | 全池冷却时网关内短等而非立刻 429 |
| `inboundTargetRpm` / `inboundRpmMax` | **614 / 300** | 🔴 差距从 307 翻到 614，见 3.2 |
| `inboundQueueTimeoutPassthrough` | true | 排队 5s 后**放行**，整形层从不拒绝 |
| `credentialRpmLimit` | 230 | 空转（盯的是绝对速率这个错变量） |
| `upstreamRetryAbsorbEnabled` | false | 刻意，见 3.4 |
| `balanceRefreshIntervalSecs` | 1800 | 🟠 用户明确说太慢，见 3.5 |

### SOCKS 节点池：5 个，启用 4 个

```
#1  (无名)          socks5://192.220.50.26:40002    enabled=True
#5  (无名)          socks5://192.220.24.27:40002    enabled=True
#6  (无名)          socks5://192.220.24.104:40002   enabled=True
#9  (无名)          socks5://192.220.24.251:40002   enabled=True
#10 NBUS-LA-SOCKS5  socks5://38.244.34.15:40002     enabled=False   ← 我导入验证用的
```
前四个「无名」是分享链接解析上线**之前**手工加的。#10 是我用新的批量导入建的，
名字来自 `#fragment`，实测 `ok:true / exitIp:38.244.34.15 / 270ms`。

---

## 2. 本轮上线的九批（全部零空窗 + 回退验证）

提交在 `origin/deploy/vps`，从旧到新：

| 提交 | 内容 | 修的是什么 |
|---|---|---|
| `6a79316` | 禁用策略收口 + failover 排除集 + 停机落盘 + region 探测接线 | 见 2.1 |
| `779f6f9` | `autoDisableSuspicious` 补齐接线 | 见 2.2 |
| `cae4721` | 按号爬坡限制（`ramp_tier`） | 见 2.3 |
| `839e783` | 限流冷却缩放下限（`scale_floor`） | 见 2.4 |
| `f6b92f8` | REST 端点 US/EU 双候选回退 + 启动 slow-start | 见 2.5 |
| `844c9f3` | 全池冷却兜底放行（P0-1） | 见 2.6 |
| `a44612a` | 分身继承探测到的 region + 补 `apiRegion` 端点 | 见 2.7 |
| `de7784e` | 额度耗尽去掉 402 门控 | 见 2.8 |
| `f1abb07` | SOCKS 分享链接解析 + 整段粘贴批量导入 | 见 2.9 |

### 2.1 禁用策略收口（四条）

链条：**瞬态错误被计成永久失败 → 号被误禁 → 池掉到 0~1 个 → 剩下的号撞进上游惩罚窗口。**

1. **刷新失败按错误类型分流**（`is_refresh_error_credential_level` +
   `report_refresh_failure_classified`）。`refresh_token_locked` 内部**已对 5xx/网络
   退避重试 3 次**才上报一次，所以上报里上游抖动占绝大多数。旧代码无条件计数 ⇒
   一次 30s token 端点抖动 = 3 次计数 = 永久禁用 + 落盘。**429 明确判瞬态**。
2. **`TooManyRefreshFailures` 纳入 `is_self_healable_reason`** —— 它与
   `InvalidRefreshToken` 是两个信号，此前一起被排除 ⇒ 全池被它打满后自愈也救不回来。
3. **`acquire_context_excluding`（排除集）** —— 此前唯一阻止 failover 重选刚失败号的是
   冷却硬门 ⇒ `cooldownEnabled=false` 时 **failover 事实上不存在**。现在是结构保证。
   ⚠️ 三条不变量写在函数注释里，改之前必读（排除是偏好非硬门 /
   `transient_wait_outcome` 不吃排除集 / 亲和旁路也要排除）。
4. **停机 `flush_stats_now()` + 每号首次成功绕过 debounce 落盘** ——
   `has_ever_succeeded` 读的是从 stats 恢复的 `success_count`，而它是 provider 判
   「bearer-invalid 403 = 瞬态 or 真 region 错配」的唯一判据。当日 41 次 SIGTERM 里
   39 次走到 SIGKILL ⇒ 成功记录丢失 ⇒ 重启后健康号被判「从未成功过」⇒ 三次瞬态 403
   即禁用。**实测 20:20:30 启动、20:20:32 打死一个 93.9% 成功率的号。**

### 2.2 `autoDisableSuspicious` 补齐接线（+ 一条运维铁律）

该字段此前**只存在于 `config.json` 与 `TokenManager`**：`reload_config` 确实在读它，
但 `admin/types.rs` 无响应/请求字段、`service.rs` 无 TIER1 更新分支 ⇒
**面板既看不到也改不了**，配置 API 读回 `None`。线上因此得出过错误结论
（以为三个自动禁用开关都被关了，其实这一项**改不到**）。

补 5 处（types×2 / service×2 / 前端 types+settings + i18n 三语各 2 键）。

> ⚠️ **`autoDisableSuspicious=true` + `cooldownEnabled=false` 是危险组合。**
> `report_suspicious_activity` 里的**计数与禁用是恒执行的**（只有冷却与族级连坐受门控）
> ⇒ 关掉冷却会让更多请求打到已 suspend 的号 ⇒ 403 攒得更快 ⇒ **更容易**撞 6 次阈值。
> **要开自动禁用就同时开冷却。** 现在两个都是 true。

### 2.3 按号爬坡限制 —— 429 的真正机制

**上游惩罚的是速率的跃升，不是绝对吞吐。** 按「凭据 × 分钟」配对，控制
**前一分钟完全无 429**（排除「429 快返回 + shield 重试 ⇒ 计数虚高」这个反向因果）：

| 本分钟 / 前一分钟 | 429 率 |
|---|---|
| **≥5x 跃升** | **48.3%** |
| 2–5x | 5.4% |
| **平稳** | **0.7%** |

与绝对速率交叉制表，每档内跃升都是主因：**100+ req/min 平缓上量只有 2.9%，
而 <50 req/min 突然跃升有 36.4%。**

新增 `RpmTracker::ramp_counts_for` + 排序键 `ramp_tier`（第⑤位，
在 `health_tier` 之后、`inflight` 之前）。

> ⚠️ **`ramp_tier` 必须排在 `inflight` 之前**（有源码守卫钉着）：429 在 ~1s 返回、
> 成功要 3s+，所以**正在被打爆的号 `inflight` 反而恒低**（实测 507 是 97% 429 /
> inflight=1，健康的 508 是 0% 429 / inflight=13）。`inflight` 是升序键 ⇒ 让它先排
> 会形成「失败越快 → 越显得空闲 → 越被优先选中」的正反馈。
> 这与 ZyphrZero v0.7.1 那个 `success_count` 补丁（注释原话"劫贫济富"）是同一类缺陷。

**已验证有效**：#509 于 23:42 入池，`2 → 2 → 1 → 6 → 18` 五分钟逐步加量、全程 0 个 429。
**已知缺口**：单号可用时它无事可做（排序键只能重新分配、不能降低总量）。
用户已明确决定**不加硬限**。

### 2.4 限流冷却缩放下限

`cooldownScalePct=10` 会把 `RateLimitExceeded` 的 15s 基线缩成 **1.5s** ——
上游那是账号级惩罚窗口，1.5s 后回池等于立刻再撞一次。
新增 `scale_floor`：`RateLimitExceeded` 8s / `SuspiciousActivity` 12s，其余返 `None`。

取 8s 而非对照实现的 60s：池子只有 1~4 个号，而排除集已保证单请求内换号不依赖冷却；
60s × 4 号 = 全池同时冷却 → 网关自造 503，正是对照实现注释里警告的形态。
**实测生效**：日志里 `冷却 8s` × 1978 次（若无 floor 会是 6s）。

### 2.5 REST 端点 US/EU 双候选回退（学 ZyphrZero v0.7.1）

实测 `management.{region}.kiro.dev` 与 `runtime.*` **只在 us-east-1 与 eu-central-1
解析**，其余 13 区 DNS 不通。而 `get_usage_limits` 只取一个 region、**无任何回退** ⇒
SSO region 是别的区的账号（Enterprise / IdC 常见）必然 403，且上游文案是
`Invalid token` —— 会让人误判成 token 坏了。

新增 `rest_api_region_candidates(sso_region)`：按前缀选主端点 + 另一个作 403 回退。
**只对 403 回退**（401 是 token 真废、429 是限流，换端点无意义，回退只会把失败往返翻倍）。

同时把 `region_probe::PROBE_ORDER` 从 5 项收窄到 2 项，`MAX_PROBE_ATTEMPTS`
从写死 3 改为 `PROBE_ORDER.len()`。
> ⚠️ **这里不变量方向反转了**：原断言是「上限 < 表长」（意图：别探满），
> 表收窄后那个意图会变成「永远探不到第二个候选」⇒ eu 账号拿不到回退机会。
> 故改为「上限 == 表长」，测试注释写明了这次反转的理由。

**启动 slow-start**（`boot_ramp_rpm`）：有效 RPM 从 25% 线性升到 100%，历时 60s。
`RpmTracker` 是纯内存的 ⇒ 每次重启爬坡历史清零 ⇒ 重启后第一分钟爬坡完全不设防。
是**读时乘数**，不写 `target_rpm`，故 AIMD 状态不受影响。
⚠️ **上线后未被验证触发**：触发线是 25% × `inboundTargetRpm`，而部署时流量低于它。

### 2.6 全池冷却兜底放行（P0-1，本轮最高价值）

`acquire_context` 在所有号都处于冷却时直接 `bail` ⇒ 客户端拿到**网关自造的 429**，
上游根本没被请求过。实测 `credential_id IS NULL` 逐小时占比：
20 点 8.2% / 21 点 9.5% / **22 点 15.3%**。

新增 `select_ignoring_cooldown`，插在 `WaitOutcome::Wait(_, Cooling)` 分支**顶部**
（因此同时覆盖 fast-fail 与等待超时两条 bail 路径）。

实现方式是给 `is_entry_selectable` 加 `ignore_cooldown` 参数（拆出
`is_entry_selectable_inner`），**而不是**重写一遍过滤。
> ⚠️ **必须复用判据函数**：两处过滤条件一旦漂移，后果不是"少选一个号"，而是
> `acquire_context` **忙等死循环**（烧满一核 + 请求永不返回）。这个缺陷本仓库真实发生过
> （见 `transient_wait_outcome` 的长注释）。复用使它结构上不可能重演。

三条不变量：绝不放行 `disabled` 号 / 只放宽冷却其余硬门全保留 / 排除集仍生效。

**实测触发 2067 次 / 50 分钟** —— 抽样看紧邻日志：约 40% 仍拿到真实上游 429，
其余成功。这是设计意图（**真实 429 带 `Retry-After`，好过网关自造的**），
但也说明**冷却门实际上已经不再拦任何东西**，接手时值得重新评估这个取舍。

### 2.7 分身继承 region（🔴 这是我自己引入又修掉的缺陷）

现场事故（02:42）：一次 `copies=5` 的多开，父号 #525 被 region 探测写上
`eu-central-1` 并 95% 成功，而同批 4 个分身 #526–529 全部 `apiRegion=None` ⇒
回退 `config.region=us-east-1` ⇒ `ksk_` 按区授权 ⇒ 恒 403 `bearer token invalid` ⇒
**24 秒内三次失败全部被禁用、0% 成功**。

根因：
```
:1386  第 1 份入池 → credential_id
:1410  probe_and_persist_api_region(credential_id)   ← 只写 entry
:1503  for seq in 2..=copies { let mut copy = new_cred.clone(); }
                                   ↑ new_cred 是局部变量，探测没写它
```

> ⚠️ **这个缺陷是接入 region 探测才引入的**：接入之前父子都没 region、**一起废**
> （症状一致，一眼是 region 问题）；接入之后变成「父好子坏」，反而更容易被误判成
> 「这个 key 不支持分身」—— 仓里注释记着上一次就是这么误判的。

修法：探测后把 entry 里的 `api_region` 读回来写进 `new_cred`。源码守卫钉住
「在探测之后、在分身循环之前」两个相对顺序。

**顺带补了 `POST /credentials/{id}/api-region`** —— 修那 4 个死号时发现
**全仓没有任何修改 `apiRegion` 的入口**（`/regions` 与 `/switch-region` 都是 ARN 门控，
只对 external_idp 有意义）⇒ api_key 号 region 错了只能删号重建。
带 `is_supported_region` 白名单校验（实测能拦住 `evil.com/x`）。

### 2.8 额度耗尽去掉 402 门控（🔴 用户报的「400 没自动禁用」的真因）

```
6 小时窗口:  402 Payment Required                        0 次
            400 + "reason":"OVERAGE_REQUEST_LIMIT_EXCEEDED"  564 次
```

两处都是 `status == 402 && is_monthly_request_limit(&body)` ⇒ **那道 402 门从不成立**
⇒ 564 个额度耗尽的请求落到通用 400 分支 `break`，凭据**不禁用、继续留在轮转里**，
每个新请求再撞一次（实测 #508 一个号吃了 543 次）。改成只看 body 的 reason 字面量。

**刻意未改的另两类 400**（都是请求级，禁用会让一个坏请求打垮全池）：
- `REQUEST_BODY_INVALID`（Improperly formed）128 次 —— 这个签名在本仓库**有前科**：
  CJK 工具名按字节超限导致上游 400，成因是**我们自己 converter 写错**
  （CLAUDE.md 已知问题 #15）。禁号会掩盖真 bug。
- `CONTENT_LENGTH_EXCEEDS_THRESHOLD`（Input too long）14 次 —— 单请求体实测 11.6MB。

### 2.9 SOCKS 分享链接解析 + 整段粘贴批量导入

节点商下发的是 `socks://base64(user:pass)@host:port#name`，而
`split_proxy_credentials` 只做**百分号解码** ⇒ 整个 base64 串被当成**用户名**
（里面没有 `:`）、密码为 None ⇒ 代理认证必然失败。**而那个失败长得像「节点不通」**，
会把排障带偏。`#US-1-SOCKS5` 还会残留在 URL 里污染 host。

新增 `parse_proxy_link` + `parse_proxy_links_bulk`（`http_client.rs`）。
判据顺序是承重的：
1. 先剥 `#fragment`（否则污染 host，`40002#US-1` 不是合法端口）
2. userinfo 含 `:` ⇒ 当**明文**，不试 base64。理由：明文密码完全可能恰好是合法
   base64，先试会解成乱码，而认证失败同样长得像"节点不通"
3. 不含 `:` 且 base64 能解出含 `:` 的 UTF-8 ⇒ 当 base64
4. 都不满足 ⇒ 当纯用户名（保持旧行为）

host:port 做**结构校验**（host 字符集 + port 1..=65535）：只判「含 `:`」不够 ——
节点商文档里的 `端口  : 40002` 同样含 `:`，无 scheme 兜底会把它当 host:port ⇒
造出 host="端口" 的假节点。**这条是写测试时才发现的，第一版确实会解析出假节点。**

base64 解码手写 12 行查表（同时接受标准表与 URL-safe 表），不为一个解码函数
把 `base64` crate 拉进生产依赖树。

`POST /socks/nodes/bulk-import`：逐行解析、非链接行安静跳过、按 url 去重、
已存在的**跳过而不覆盖**（覆盖会把已测活启用的节点重置成未启用）、
SSRF 逐条校验只跳过该条、**默认 `enabled=false`**（用户明确要求）。

**实测**：粘贴节点商完整文档 → `added:0 duplicate:5 skipped:11`（正确识别 5 个已存在、
跳过 11 行说明文字）；删掉 #8 单行重导 → `name='NBUS-LA-SOCKS5' user='nbusu'
pass=已设 enabled=False`，测活 `ok:true exitIp:38.244.34.15 270ms`。

---

## 3. 🔴 待办清单（按优先级，给下一个 AI）

### 3.1 🔴 前端：分身管理三件（用户本轮明确要求，**未做**）

前端组件 `admin-ui/src/components/clone-management-card.tsx`（542 行）已存在，
后端 `POST /credentials/{id}/clone` 端点也已存在（`service.rs::clone_credential`）。
缺的全是 UI：

1. **删除分身按钮** —— 单个分身删除。后端用现有 `DELETE /credentials/{id}`。
   ⚠️ 删分身**不要**动同组其它成员，也不要动节点池（节点是候选池、凭据的 `proxy_*`
   是绑定结果，两者独立）。
2. **分身组里「选凭据生成」** —— 点按钮 → 选一个池中已有凭据 → 选份数 → 调
   `POST /credentials/{id}/clone {copies: N}`。
   参考「凭据设置」里已有的选择器形态（用户指名要那个 UI）。
3. **「生成分身时是否全部默认启用」开关** —— 默认**关**（用户明确要求）。
   ⚠️ 这需要后端配合：现在 `clone_credential` 建出来的分身是启用的。
   要么加 `enabled` 到 `AddCredentialRequest`，要么建完立刻批量禁用。
   **建议前者**（后者有中间窗口，分身会先接一波流量）。

### 3.2 🔴 SOCKS 节点前端可编辑（用户本轮明确要求，**未做**）

后端已通（`upsert_socks_node` 现在接受分享链接，`bulk-import` 已上线）。
用户要的是**统一走「代理」那个表单形态**：
> 「留空=全局，"direct"=不走代理，可账密内嵌 URL 自动拆分。下次请求生效。[测活] [保存]」

即节点编辑器应该复用凭据代理设置那个组件的交互（一个输入框 + 测活 + 保存），
而不是现在的多字段表单。**新增一个「批量导入」文本域**调 `bulk-import` 端点。

⚠️ i18n 三份（`admin-ui/src/i18n/resources/{en,ja,zh}.json`）：
**扁平点号键、按键名字典序排**，不是嵌套结构。加完必须校验三份键数相同且差集为空。
占位符是**单括号** `{var}`，双括号会显示字面量。用户可见文案里不要用 markdown `**`。

### 3.3 🟠 `inboundTargetRpm` / `inboundRpmMax` 打架（P1-2，本轮恶化了）

```
inboundTargetRpm = 614     ← autotune 在往上调（本轮从 307 涨到 614）
inboundRpmMax    = 300     ← throttle.rs:89 把 target 夹到这里
```
**实际生效 300，而 autotune 拿存储值 614 跟自己的建议比 ⇒ 死区永远满足 ⇒ 永远调不动。**
本轮差距从 307 翻到 614，还在恶化。

最小修法（零代码）：让 `inboundRpmMax >= inboundTargetRpm`。
真修法：`throttle.rs` 的夹取语义与 autotune 的比较基准统一到**生效值**。
⚠️ `inboundQueueTimeoutPassthrough` 改 false 是吞吐换成功率的取舍，**需人拍板，别自行决定**。

### 3.4 🟠 额度/积分刷新太慢（用户本轮明确提出，**未做**）

现在 `balanceRefreshIntervalSecs=1800`（30 分钟温和刷新），面板长时间显示「暂无数据」。
用户要求参考 kiro-rs 的刷新逻辑。

参考实现在 `~/Documents/Project/_study/kiro-rs/src/admin/`（那份是 ZyphrZero v0.7.1，
VPS 上跑的那份带本地补丁）。**先读它怎么做的再动**，注意两件事：
- `web_portal` 是上游探测，调多了会加重风控（本仓库调优结论：绝不为展示类需求每请求打它）
- 我们的 `get_usage_limits` 现在有 US/EU 双候选回退，刷新逻辑要复用它而不是自己拼 host

### 3.5 🟠 吸收层三条未修（默认关，手动开之前必须修）

1. 可吸收类别实际约 **18%**（不是 38%）—— 池空那 16.5% 的自愈退避下限 60s >
   吸收总预算 45s，**结构上**吸收不了
2. `ABSOLUTE_MAX_TOTAL_RETRIES=12` 语义是「**每轮**」而非「每请求」：`max_retries` 在
   `'absorb: loop` 外算一次、循环每轮重跑 ⇒ `max_rounds=3` 时最坏 (1+3)×12 = **48 次**
   上游调用、同一出口 IP
3. 轮次准入闸门比的是被 clamp 到 `max_delay`(15s) 的退避，而非号池真实恢复时间
   ⇒ 睡 15s 醒来池子还在冷却，白打一轮

已有**联锁守卫**：只要 `max_retries` 还在循环外，就断言吸收层必须默认关；
翻默认值即 FAILED。**正解是让吸收轮共享跨轮次数预算，别只改默认值。**

### 3.6 🟠 shield 与三个 unit 不在任何仓库

`kiro_shield.py`（612 行）+ `kiro-shield.service` + `socks-rotator.service` +
`kiro-ratelimit-monitor.service`，**全部只在 VPS 上 untracked**。

它本轮 224 resolved / 733 attempts / **放大 3.27x / gave_up 0** —— 是目前
唯一在吸收上游 429 的东西。**按现状 clone 重建机器不会有它。** 应纳入 ws-vps 仓库。
**不要停它，不要改 Caddy 指向。**

### 3.7 🟡 池内四个号全靠 `config.region` 回退

见 1 节。**改 `config.region` 会让四个号同时全废。**
建议用 `POST /credentials/{id}/api-region`（本轮新增）逐个探好写死。
探测判据：**200 或 429 = region 对；403 = region 错；401 = token 废。**
（429 判可用是承重的：说明请求到了正确的区只是拥堵。）

### 3.8 🟡 已知问题 #20 / #21 原封未动

- **#20**：`acquire_admission()` 超时既不 `emit_record` 也不 bump 计数器 ⇒
  被整形层掐掉的请求在面板上**不存在** ⇒ 看到的成功率**偏乐观**。
  而"面板成功率"是后续一切调参判断的依据。
  ⚠️ 坑：`acquire_admission()` 在 `call_api_with_retry` **内部**，45s 墙钟也在它内部
  ⇒ **不要在这里加重试**，只让它可观测。
- **#21**：`retries` 无法聚合（`Aggregate` 无 retry 字段、`add()` 不读 `r.retries`）。
  数据是齐的（4 处写入点 + provider 失败路径），只是聚合层不读。
  前端类型有**两份独立定义**（`types/api.ts` 与 `api/ops.ts`），加字段要两边一起改。

### 3.9 🟡 `thinking` tag 泄漏（另一个会话在做，**不要碰**）

`src/anthropic/stream.rs` 有 205 行未提交改动，是另一个会话在修「thinking 标签泄漏
导致工具静默不执行」。他自报：
- `QUOTE_CHARS` 从 30 个字符收窄到 3 个（` " '）
- `find_real_thinking_end_tag` 后缀判据放宽
- 新增 `thinking_end_tag_consumed_len` 替掉 3 处写死的 13 字节
- 他自己也踩了纸面测试（第一版只断言辅助函数，改回写死 13 仍全绿）

他明确说**两个已确认但没修的缺陷**：`generate_final_events` 的收尾 flush 被
`thinking_enabled` 门控而 `strip_inline_thinking_when_disabled` 往同一个 buffer 写
⇒ thinking 关闭时残留蒸发；非流式路径压根没有剥离逻辑。

**接手后先确认这个会话还在不在。** 如果已经走了、改动还在工作树，
需要有人跑一遍验证再决定上不上线。

### 3.10 ⚪ P3 结构性

`src/kiro/token_manager.rs` 已 **12,700+ 行**单文件（kiro-rs 同名文件 4,748 行）。
两个会话并发改它冲突风险高。本轮我独占它做了 5 批改动。

---

## 4. 「只要 403 和 400 就禁用」—— 我按意图实现，没按字面

用户本轮要求「只要 403 和 400 就直接禁用，其他不要禁用」。**字面实现会重演本轮修掉的事故**：

```
#481   4545 次成功 / 91.1% 成功率 / 但吃过 15 次 403
```
见 403 就禁用 ⇒ 它在第一个 403 就死，而它当时是全池最好的号。那正是
「误禁 → 池子掉到 1 个 → 429 风暴」这条链的起点。

**现在的判据**：
- **403 类**：`autoDisableSuspicious=true` + 连续 **6** 次账户级 403 **且期间零成功**
  → 禁用。任意一次成功即清零 ⇒ 健康号永远到不了阈值；真死号（实测那两个吃了
  2906 / 1622 次连续 403）会被禁。
- **400 类**：只有 `OVERAGE_REQUEST_LIMIT_EXCEEDED`（号级终态）禁用。
  另两类（`REQUEST_BODY_INVALID` / `CONTENT_LENGTH_EXCEEDS_THRESHOLD`）是请求级，
  禁用会让**一个坏请求打垮全池**。

用户若仍要一律禁，**加开关让他自己选，但默认必须是现在这样**，并把上面这个反例
一起摆给他。

---

## 5. 已被证伪的机制（别重复，全部有数据）

| 假设 | 为什么错 |
|---|---|
| 429 随请求体增大 | **`input_tokens` 来自上游 usage，429 请求永远拿不到 ⇒ 17550/17550 条 `rate_limited` 的 `input_tokens=0`。** size-bucket 表只是在区分"失败/成功" |
| 并发数决定 429 | 429 行平均在飞 6.45、成功行 3.51，看着很强。但**只按成功请求算负载**后完全反过来：0 个成功在飞时 429 率 81.6%、5–8 个时 3.4%。那个量测的是「该号是否正处于惩罚窗口」，**是结果不是原因** |
| token 量/分钟有天花板 | #481 单分钟 **2270 万** input tokens 零 429 |
| region 决定 429 | 控制时间后 eu 号同样 55–62%。**region 决定 403，不决定 429** |
| 端点（IDE vs CLI）决定 429 | 两个都干净（454 IDE 0.1% / 473 CLI 0%） |
| 「调度器把 15× 流量发给 97% 失败的号」 | **我自己报错的。** 逐分钟拆开看，507 当时是池里**唯一**的号。**读跨池子变动的累计数**这个混淆本轮咬了我**六次** |
| 「kiro-rs 同号 180 RPM 零 429 而我们限速」 | 那是**逐步加压**的压测，全程在「平稳」档（见 2.3）。且两边同 30 分钟窗口按 upstream attempt 口径：kiro-rs 540 ok / 1695 × 403 / **0 × 429**；我们 605 ok / 1239 × 403 / 0 × 429 ⇒ **24% vs 29%，我们略优**。它把 403 记进 `auth_failed` 桶，我们记 `rate_limited`，**是记账口径差异不是限速差异** |

### 与 ZyphrZero kiro.rs v0.7.1 的对照结论

VPS 上那份是 `version 0.7.1` + 本地补丁（源码在 `~/Documents/Project/_study/kiro-rs`）。

**他们对 403 的处理严格比我们差 —— 别照搬**：`provider.rs:620` 的 `401 | 403` 分支
直接 `report_failure`（3 次即禁用），账号风控分支在它**之后**且门控在 `status == 429`，
而 `default_account_throttle_kind` **不认 `temporarily is suspended`**。
线上占主导的那 3749 次 403 全部落进凭据失败路径 —— 这正是他们 DB 里
1737 `auth_failed` + 905 全池禁用的来源。**那是我们刚修掉的缺陷。**

**已抄的三样**：REST 端点 US/EU 回退（2.5）、分级冷却下限（2.4）、
`PROBE_ORDER` 收窄（2.5）。
**我们比他们强的两处**：`effective_upstream_region` 多两层优先级
（`profileArn` 严格校验 + `sanitized_region` 白名单），kiro-rs 填什么就往什么区打。

---

## 6. 流程教训（这一节值得读）

「回退即 FAILED」验证本轮抓出我**四个纸面测试**：

1. **排除集的行为测试** —— 两道串联 filter 互相掩护，删掉任一条都仍绿。
   拆成「关亲和隔离 filter」+「亲和旁路改源码守卫」，并在注释里写明为什么后者
   只能是源码守卫（`available` 已被前一道 filter 剔过，`find` 必然 `None`）。
2. **首次成功落盘** —— 没预热 debounce 时钟，而 `last_stats_save_at=None` 时
   `save_stats_debounced` 本来就会立刻落盘 ⇒ 删掉修复也绿。已加预热。
3. **`try_take` 是否真用了 boot ramp** —— 拿传入的 1200 算期望值，而 `mk()` 会把它
   clamp 到 `rpm_max=300` ⇒ 两种实现都远小于期望 ⇒ 断言恒真。改为从
   `current_target_rpm()` 取实际生效值后，回退即 FAILED（实得 40 vs 折算后 24）。
4. **分身 region 回写的位置守卫** —— 裸用 `"for seq in 2..=copies"` 做锚点，
   而本文件有**两处注释**在散文里提到这个循环 ⇒ `find` 先命中注释 ⇒ 位置比较反向
   ⇒ 守卫静默通过。改为带上循环体左花括号。

**共性**：`include_str!` 源码守卫的 needle 必须**运行时拼接**，位置比较前要按
`#[cfg(test)]` 切掉测试段，而且**锚点要选代码而不是可能出现在注释里的散文**。

另外：**我自己引入了一个缺陷**（2.7 的分身 region），它是接入 region 探测的副作用，
而且症状比修复前**更难判断**（父好子坏 vs 父子一起废）。接手时对「新加的机制是否
让某条路径的症状变得更难归因」保持警惕。

---

## 7. 环境与验证

```bash
# ⚠️ 必须先构建前端：rust-embed 编译期嵌入 admin-ui/dist，缺 dist 时 cargo 报 E0599
cd admin-ui && pnpm install --frozen-lockfile && pnpm build && cd ..

# ⚠️ 一律加 --no-default-features（Cargo.toml 的 default=["native-tls"] 与出厂配置相反）
cargo test --no-default-features --bin kirostudio      # 本轮 1121 全绿
cargo clippy --no-default-features --bin kirostudio    # 0 error
cd admin-ui && npx tsc --noEmit                        # 干净
```

### 部署流程（本轮跑通 5 次，零空窗）

```bash
gh workflow run deploy-build.yml --repo dwgx/KiroStudio-skiapi --ref deploy/vps -f run_tests=true
# 等 completed/success（约 5 分钟）
gh run download <RUN_ID> --repo dwgx/KiroStudio-skiapi -D /tmp/dl
BIN=/tmp/dl/kirostudio-linux-x86_64/kirostudio-linux-x86_64
shasum -a 256 "$BIN"; cat /tmp/dl/*/*.sha256          # 三处 sha256 必须一致
scp "$BIN" ws-vps:/tmp/ks.new && ssh ws-vps 'sha256sum /tmp/ks.new'
ssh ws-vps 'cp -a /opt/kirostudio/bin/kirostudio /opt/kirostudio/bin/kirostudio.rollback-<tag>'
ssh ws-vps 'chmod +x /tmp/ks.new && /tmp/hotswap.sh /tmp/ks.new check'   # 先 check
ssh ws-vps '/tmp/hotswap.sh /tmp/ks.new'                                 # 再真交接
```

验证（缺一不可）：
```bash
ssh ws-vps 'ls -l /proc/*/exe 2>/dev/null | grep -c "kirostudio$"'      # 应为 1
ssh ws-vps 'sha256sum /opt/kirostudio/bin/kirostudio | cut -c1-16'      # 应等于新版
ssh ws-vps 'journalctl -u kirostudio --since "3 min ago" | grep -ci panic'  # 0
```

回滚点（本轮建的）：
```
kirostudio.rollback-pre-socks           ← 最新（f1abb07 之前）
kirostudio.rollback-pre-clone-region
kirostudio.rollback-pre-p0-1
kirostudio.rollback-pre-region-bootramp
kirostudio.rollback-pre-ramp
kirostudio.rollback-pre-autodisable
kirostudio.prev                          ← hotswap 自动建的最近一个
```

### 线上只读排查（比推理便宜 100 倍）

```bash
# 逐分钟：池子大小 vs 429（本轮核心证据）—— ⚠️ 必须按 ts_ms 过滤，
# 不要用 strftime("%H:%M")=... 那会跨天聚合（本轮踩过）
ssh ws-vps 'sqlite3 -column -header /opt/kirostudio/data/usage/traces.db "
  select strftime(\"%H:%M\",ts_ms/1000,\"unixepoch\",\"localtime\") m, count(*) n,
    sum(outcome=\"success\") ok, sum(outcome=\"rate_limited\") rl,
    sum(credential_id is null) nocred, count(distinct credential_id) creds
  from traces where ts_ms>(strftime(\"%s\",\"now\")-1800)*1000 group by 1 order by 1;"'

# ⚠️ rate_limited 必须拆开看：一半可能是 POOL_EMPTY（credential_id IS NULL），
# 那是网关自造的，上游根本没被请求过

# kiro-rs 的真实上游健康（它自己的 DB，有 per-attempt 表）
ssh ws-vps 'sqlite3 -column /opt/kiro-rs-test/run/traces.db "
  select a.outcome, a.http_status, count(*) from trace_attempts a
  join traces t on t.trace_id=a.trace_id
  where t.ts_epoch>(strftime(\"%s\",\"now\")-3600) group by 1,2 order by 3 desc;"'
```

⚠️ `gateway-status` **会假绿**：只看「是否在冷却中」不看「拿去用能否成功」。
⚠️ **SSH 别刷太快**：连打十几次会被 fail2ban 拦几分钟。
⚠️ 本机 `date` 返回 JST，服务器是 CST，差一小时。SQL 一律用服务器本地时区。

---

## 8. 一句话总结

**本轮九批零空窗上线，把「瞬态错误被计成永久失败 → 号被误禁 → 池子掉到 1 个 →
那个号撞惩罚窗口 → 429 风暴」这条链从起点切断，并找到 429 的真正机制
（惩罚速率跃升而非绝对吞吐，≥5x 跃升 48.3% vs 平稳 0.7%）。
线上从「1 个号 / 27% 成功」恢复到「4 个号 / 近 10 分钟 ~100% 成功、0 个 429」。**

**但用户本轮明确要的前端三件（分身删除 / 选凭据生成 / 节点可编辑）一件都没做
（见 3.1、3.2），额度刷新太慢也没动（3.4）。subagent 全程不可用（18 个启动即死），
所以全是串行做的，前端那批留给你。**
