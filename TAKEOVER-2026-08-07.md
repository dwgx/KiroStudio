# KiroStudio 接手状态（2026-08-07）

## 先说结论

这是当前 `/Users/dwgx/Documents/Project` 里风险最高的仓库：生产正在运行的二进制、`HEAD`、
工作树和十几份历史 handoff 不是同一个状态。

当前扫描看到：分支为 `master`，最新提交是 2026-08-03 的 `a066fdf`；工作树同时有文档、Rust、
前端源码和测试改动。`STATUS.md` 中记录的“143 个未提交项”是它自己的快照，当前重新扫描出的
数量已经不同，不能直接拿来做部署判据。

## 可信度分层

| 层 | 可用作什么 | 不能用作什么 |
|---|---|---|
| 当前代码 + 当前测试 | 判断工作树中是否存在实现和回归覆盖 | 不能证明已经部署 |
| `HEAD` / `deploy/vps` | 判断可重现的 Git 快照 | 不能代表工作树中的未提交修复 |
| VPS 实读 | 判断线上状态 | 不能替代本地测试或审查 |
| `STATUS.md` | 导航和历史依据 | 数字、配置、池容量必须重新读取 |
| `HANDOFF-*` / `PLAN-*` | 理解推理过程和事故 | 不可直接当当前任务清单 |

## 已能确认的状态

- `STATUS.md` 已明确承认：最近一整天的修复主要只在工作树，没有进入 `HEAD`。
- 用户报的分身分组、画布观感、重启字段恢复、工具输入合并、错误分类和若干前端能力，
  都有“代码/测试已经写到树里”的记录；这些必须在部署前重新跑一遍，不得把测试名当成上线事实。
- 生产侧仍有外部运维仓、Caddy/shield、VPS 脚本和线上配置；仅改本仓库文档不能完成生产闭环。

## 当前未完成 / 未确认

1. 未提交工作树批次尚未被安全地隔离成可复现快照，也没有完成一次“工作树不变”的部署前核对。
2. `kiro-monitor` 的分钟级产出恢复和数据新鲜度检查在当前入口仍标为待处理。
3. L3 cache 的真实命中上限受 `tool_result` / `tools` 红线影响，需要 owner 决定是否放宽。
4. Codex `previous_response_id` 尚未实现。
5. IP 池闭环、节点健康、出口碰撞和 IP 池 UI 尚未完成。
6. CLI endpoint / `origin` 差异、图片 magic bytes、thinking 关闭时空响应、SOCKS 编辑等仍需逐项验证或实现。
7. “观感变好”不能由源码尺寸变化证明，画布/面板必须做人眼或截图验收。

## 接手动作

1. 先重新记录：`git status --short`、当前分支、`git log -1`、测试数、未跟踪文件数。
2. 不用 `checkout`、`reset`、`stash`、全仓 `cargo fmt` 或真实 index 做快照。
3. 只把一批明确的路径放进临时 index，先生成树和 diffstat，再决定是否部署。
4. 本地构建继续使用前端先 build、Rust `--no-default-features`；线上不要直接编译。
5. 部署后必须同时检查二进制校验、实例数、panic、真实请求结局、号池和数据新鲜度。

## 文档规则

本文件是 2026-08-07 的接手摘要；`STATUS.md` 仍是仓内原有入口，历史 handoff 继续保留作证据。
任何新事实应先写本文件或 `STATUS.md`，并把旧结论显式标为历史/已推翻，不能在旧 handoff 末尾再追加
一段新的“当前状态”。
