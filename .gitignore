# 参考库：11 个外部项目的本地副本，不纳入本仓库
/reference/

# 凭据与密钥（绝不提交）
*.env
.env*
credentials.json
config.json
*.key
*.pem
secrets/

# 数据库
*.db
*.sqlite
*.sqlite3

# Rust
/target/
**/target/
Cargo.lock.bak

# Node / 前端
node_modules/
dist/
build/
.vite/

# 系统/编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# CodeGraph 索引（本地产物）
.codegraph/

# npm 工件（本项目用 pnpm）
admin-ui/package-lock.json
*.tsbuildinfo
.playwright-mcp/

# 临时部署产物
*.tar.gz
*.zip
dist-windows/
data/
ui-mockups/
*.txt.dproj.txt
tmp-import*/

# 临时实测脚本 / 本地命令 caveat 转储（不进 repo）
_*.sh
*-local-command-caveat*.txt

# 归档目录：老文档统一归此,整目录不进公开 repo(含敏感历史交接/运营文档,仅留本地)
docs/archive/

# ⚠️ 敏感运营文档：绝不推公开 repo（含真实账户邮箱/M365租户GUID/AWS账户号/SSH路径/
# adminkey 线索/号池防封策略/内部交接细节）。仅留本地。可公开的纯技术文档单独列白名单在下方。
docs/HANDOFF-*
docs/PLAN-*
docs/DESIGN-M365-*
docs/RESEARCH-*
docs/TODO-*
docs/BACKLOG.md
docs/DISCUSSION.md
docs/PROJECT-SELF.md
PROMPT.md
docs/PROMPT-*
PROMPT-*.md
/research/

# Claude Code agent 配置/记忆
#
# 2026-08-10 起入库，但**只能进私有仓 origin（github dwgx/KiroStudio-skiapi）**。
# 这些文件含生产机 IP、SSH 端口、密钥文件名、线上配置路径 —— 绝不能进
# public（github dwgx/KiroStudio）或 gitee。
#
# 已做的机械保护：
#   - master 的上游已从 public/master 切到 origin/master，`git push` 默认走私有仓
#   - public 的 push URL 已设为 DISABLED_use_origin_instead，推它会直接报错
# 要给 public 发版就单独 checkout 一个不含这些文件的分支，别解除上面两条。
#
# 仍然不入库的：会话级中间产物和运行时文件，换机器没意义
.claude/state/
.claude/settings.local.json
.claude/scheduled_tasks.lock
kiro_balance_cache.json

# 运行时产物 / 本地临时(绝不提交)
kiro_stats.json
replay.tmp.json
scripts/
