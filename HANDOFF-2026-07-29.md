# KiroStudio 交接文档 — 2026-07-29

> 给下一个接手的 AI。**先完整读这份，再动任何代码。**
> 配套读 `MASTERPLAN-2026-07-29.md`（分批实施计划的完整版，含每批的改动清单/测试/验证/回滚）。

---

## 0. 三条硬约束（先看这个，违反会造成真实损失）

1. **工作树有 47 个其他会话的未提交改动。**
   禁止 `git checkout` / `switch` / `stash` / `reset` / `commit` / `add`，禁止全仓 `cargo fmt`
   （历史事故：有会话跑了全仓 fmt，把别人整树改动冲掉）。
   要提交/推送时用 git plumbing 在临时 index 里做快照，不碰真实 index（`CLAUDE.md` 有完整命令）。
   做完必须验证：分支名与 `git status --porcelain | wc -l` 与开始时一致。

2. **不要在 VPS 上编译。** 那台机器 4 核是瓶颈，编译会抢死正在服务的 sub2api。
   走 GitHub Actions（`deploy-build.yml`）出 musl static-pie。

3. **用户明确要求过：不要乱改。**
   本轮我犯过一次（详见第 3 节 S0），教训是：**没有实测证据支撑的改动不要做**，
   样本量不足时不要下结论。

---

## 1. 本轮已完成并已部署（线上生效）

线上二进制 sha256 `2be835f3c366f80d8aa2bc8f8388ec62b73d99a7543f7a5ef2279c09f3521ac4`，
924 个测试全绿。对应 `origin/deploy/vps` commit `36caf93`。

| # | 改动 | 文件 | 依据 |
|---|---|---|---|
| 1 | 死号自动禁用脱离 `cooldownEnabled` 门控 | `token_manager.rs::report_suspicious_activity` | 原先整个函数体被一个 `if cooldown_enabled` 包住，关掉冷却会连带关掉**识别坏号**的能力（三种不相关职责被一个开关耦合）。线上 `cooldownEnabled=false` 时实测：8 个成功率恒 0% 的死号跑数小时仍全部 `disabled=false` / `failureCount=0`，巡检假绿显示「43 个号全可用」 |
| 2 | 自动禁用判据改 `consecutive_suspicious`（连续风控且零成功，阈值 6） | 同上 + `CredentialEntry` | 旧判据 `cooldown.trigger_count >= 10` **在数学上不可达**：`report_success` 调 `clear_cooldown` → `entries.remove()` 删掉冷却条目、计数归零，半死号永远到不了阈值。**自动禁用从来没生效过** |
| 3 | `compute_max_retries` 去掉 `.max(available)` | `provider.rs` | 内层 `.max` 让 12 的硬上限在 `available>12` 时自我抵消。43 号时预算=43，日志即「尝试 43/43」→ 单请求扫全池、耗尽 45s 墙钟 → **号池越大越慢** |
| 4 | 403 分支补 `MAX_SUSPICIOUS_FAILOVERS_PER_CALL=3` | `provider.rs` | 原先只有同号去重、无跨号上限，可线性扫全池；而 `account_suspended` 分支早有等价物 |
| 5 | `RpmTracker` 改 `VecDeque` 前缀弹出 + 新增 `counts_for` 批量读 | `scheduling.rs` + `token_manager.rs::select_next_credential` | 原 `Vec` + `retain` 每次 O(w) 全扫；选号对每候选各加一次锁。43 号×200RPM 单次选号约 8600 次比较 + 43 次加锁，全在 `entries` 锁临界区内。现锁获取 O(n)→O(1)、剔除摊还 O(1) |
| 6 | 未识别事件按类型只告警一次 | `kiro/model/events/base.rs` | 线上实测 `reasoningContentEvent` 2 小时刷 **55880 条**，淹掉真实错误（之前排查 403 就是被它淹的） |

### 已写完但**未部署**

| 改动 | 文件 | 说明 |
|---|---|---|
| `shutdown_with_drain_cap`（8s drain 上限） | `main.rs` | 部署中断 74s → 约 8s。见第 2 节 502 分析。**当前工作树里有，线上二进制没有** |

---

## 2. 已查明的线上事实（不要重复调查）

### 502 的完整归因（Caddy access.log 全量扫描，403 次）

| 类别 | 次数 | 性质 |
|---|---|---|
| 部署造成 | 310 | `systemctl restart` 期间进程停 **74 秒**（实测 02:54:56→02:56:10），单次产生 167 次 502。根因 `with_graceful_shutdown` **无限**等在途 SSE drain → 只能等 systemd `TimeoutStopUSec=90s` 超时 SIGKILL。502 的 **p50 duration 仅 0.01s**（连接被瞬间拒绝，非超时）。已写修复未部署 |
| 真实故障 | 93 | p50 **20.66s** / p90 71.75s / max 1077s，长时间挂住后超时。集中在 `/v1/messages`(72)、`/v1/chat/completions`(11)，及面板 `credentials/{id}/balance`(5)、`usage/*`、`ratelimit/insights` |

### 面板卡顿的真因（不是后端查询慢）

admin API 在服务本地实测 **0.3ms**（`credentials`、`usage/overview`）。慢在前端：

- `dashboard.tsx:722` 是 `if (error)`、`settings-page.tsx:1620` 是 `if (error || !config)`
  —— React Query v5 后台 refetch 失败只置 `status:'error'` 并**保留 `data`**，
  是页面自己把缓存丢掉换成错误卡。**用户看到的「加载失败 Request failed with status code 502」就是这两行。**
- `api/usage.ts` 与 `api/ops.ts` 的 axios 实例**没有 timeout**（默认 0 = 无限等），
  只有 `api/credentials.ts` 有 15000。挂在网关那跳时会挂到上游超时，期间面板整块冻结。
- `main.tsx:18` 的 `failureCount < 1` —— 502 只重试 1 次。

### 号池实测数据

- 死号与好号**分界干净、无过渡态**：8 个死号成功率恒 **0%**（n=4~48 全 403），
  9 个好号 **90~100%**。→ 这就是「连续零成功」判据的依据。
- **403 `TEMPORARILY_SUSPENDED` 是临时态**。历史事故：曾被当永久封禁处理 →
  12 小时 88 次误禁 + 51 次「所有凭据已用尽」+ 36 次全池自愈活锁，逐小时拒绝率升到 100%。
  **判据必须是「连续失败且零成功」，绝不能是「见过 403」。**
- 分流实测 gini 0.325 / 最热最冷 5.19x，但**成因是会话亲和**（一个 session 占 35% 流量），
  不是排序键缺陷。429 次数与流量正相关（拿最多流量的 429 最多），说明没有号被不公平打爆。
- prompt cache 命中率 11%，但 `cache_read` 是**我们自己注入的估算值**（非真值），不可信。
- 压缩实测 6.88MB→6.66MB **只省 3%**，压缩后仍超上游 ~5MiB 限制，而它改写历史中段破坏前缀。

### `/api/import/keys` 接口（本轮已验证，无需改代码）

10 条兼容要求全部满足，实测逐条验过。**别名路由早就存在**
（`admin/router.rs::create_import_alias_router`，不带尾斜杠、复用同一鉴权），
三个历史二进制里都有。

- ✅ 可用地址：`https://k1ro.skiapi.dev/api/import/keys`
- ❌ 不可用：`https://api.exampledesk.cc/api/import/keys` —— 该域名解析到 **Cloudflare**
  且 CF 未回源到我们。证据：经该域名返回 `HTTP/2 404` + `server: cloudflare` + `cf-ray`；
  我们 Caddy 日志里该域名 14 条请求来源 IP **全是 `127.0.0.1` 或我们自己**，零条来自 CF 回源。
  **这不是代码问题，改代码解决不了。**
- 对接说明已交付用户：`~/Desktop/import-keys-对接说明.md`

---

## 3. ⚠️ 本轮的错误与未修完的坑（最重要的一节）

### S0 — 我写的 `decay_idle` 是空操作，线上饥饿自锁**仍然存在**

这是本轮最重要的遗留问题，也是我犯的错。

**症状**：低档号拿不到请求 → 无 `on_success` → EWMA 永不回升 → 永久留在低档。
实测修复前 6 号池里 **4 个 T2 坏档 + `rpm=0/inflight=0` 完全空转**，
有效容量 **6→3**，全程零 429。这就是「越跑越慢」的确切机制。

**我的修复为什么无效**：`p_avail_with_load_ref` 的顺序是
`tick_circuit → decay_idle → s.last_touch = now`，而它在选号时对**每个候选**都被调用
（`sort_key` 闭包里）。所以饥饿号虽然拿不到请求，`last_touch` 仍被每轮选号刷新 →
`idle` 恒 <5s → `decay_idle` 每次都在 `IDLE_DECAY_MIN_SECS` 门槛处 return。

**实测证死**：模拟 200 轮生产选号后 `ewma_429: 0.875 → 0.875`，衰减一次都没执行。

**为什么测试没抓住**：我的测试**手工回拨了 `last_touch`**
（`health.rs` 的 `s.last_touch = Instant::now() - Duration::from_secs(180)`）——
验证了衰减数学，没验证衰减能否**被执行到**。这是「绕过了自己要验证的那条读路径」的典型。

**我还错报过一次结论**：部署后我说「已验证生效：T2 坏档 0、空转 0」——
那其实是 `systemctl restart` 把所有 EWMA 重置成乐观初值 1.0，
我把重启效果当成了修复效果。**不要重复这个错误：EWMA 类状态在重启后一律是干净的，
观测修复效果必须在进程持续运行且有真实流量的窗口里做。**

**正确改法**（见 MASTERPLAN 批 3）：衰减时钟必须从 `last_touch` 拆开
（`last_touch` 语义是「键还被引用」，供 `cleanup` 的 `IDLE_EVICT_SECS` 淘汰用，
`p_avail` 刷新它是对的），新增只在**真实结果点**（`on_success` / `on_429` /
`report_family_suspicious`）更新的独立时间戳，且**去掉门槛连续衰减**
（指数衰减可组合，`dt≈0` 时 `factor≈1`，同轮天然幂等）。

### S1 — 还有三个 `decay_idle` 治不到的单向棘轮（已核实成立）

`decay_idle` 只碰 `ewma_429` / `ewma_success`。以下三个都没有下降路径：

- `open_count`：在 `health.rs` 的 `:289 :297 :325` 递增，只在 `:267`
  （HalfOpen 内连续 5 次成功）归零。而 `report_family_suspicious`（`:325`）
  对 403 **无条件** `open_count += 1` —— 而 403 是临时态。
- `admit_prob_seed`：每次半开失败 ×0.5 直到下限 `MIN_ADMIT_SEED = 0.02`。
- **死锁链**：403 → `open_count++` → 退避顶格 1800s + `admit_prob_seed=0.02`
  → `p_avail=0.02` → `health_tier=2` 排最后 → 拿不到那 2% 试探 →
  凑不齐 5 次成功 → `open_count` 永不归零 → **永久化**。

### S2 — 禁用原因重启后全变「手动禁用」（已核实成立）

用户明确要求「认定封号必须标明原因」，而这个需求**当前重启即失效**：

- `KiroCredentials` 只有 `disabled: bool`（`model/credentials.rs:203`），**没有** `disabled_reason`
- `persist_credentials` 只写 `cred.disabled = e.disabled`
- 加载时（`token_manager.rs:1566`）对所有 disabled 号一律回填 `Some(DisabledReason::Manual)`

所以 `SuspiciousActivityAuto` / `QuotaExhausted` / `AccountSuspended` / `TooManyFailures`
**重启后全变成 Manual**，并连带击穿三处以 reason 为判据的逻辑
（含全池 `TooManyFailures` 自愈 → 整池禁用后永久死锁）。

### S3 — 删除凭据必须先禁用（用户要的「强制删除」正是为此）

实测 `DELETE /credentials/229` → `400 "只能删除已禁用的凭据（请先禁用凭据 #229）"`。
删一个号要两次调用，批量删 N 个 = 2N 次往返。

### S4 — 我加的 `tie_break_jitter` 是没有证据支撑的改动

我基于 52 个样本算出 gini 0.378 就断定分流有缺陷、在 `sort_key` 加了随机打散键。
样本涨到 275+ 后我自己验证：**移除它回归测试依然通过** —— 分流本来就靠第⑧位
`success_count` 拉平了。

它现在排在**所有确定性信号之后**（第⑨位），实际影响接近零。
**用户已知情，尚未决定是否撤掉。** 建议撤（它是唯一无证据支撑的改动）。

### S5 — `compute_max_retries` 恒封顶 12 的耦合（用户已知情）

代价是：**若号池很大而前 12 个碰巧都不可用，请求会失败而不再继续找**。
它依赖「坏号被自动禁用后不进候选集」这个前提。用户加号时需知道这个耦合。

---

## 4. 未做的功能（用户明确要求过的）

按用户原话整理，完整实施方案见 `MASTERPLAN-2026-07-29.md` 对应批次：

| 用户要求 | MASTERPLAN 批次 | 状态 |
|---|---|---|
| 前端卡顿 / 「加载失败 502」/ 后台并发处理 | 批 1（前端）+ 批 4（后端读路径） | 未做，**风险最低、用户马上能感知，建议先做** |
| 「账号认定为封号了必须知道禁用、标明原因」 | 批 2 | 未做（S2 是它的阻塞项） |
| 「越跑越慢绝对不会再出现」 | 批 3 + 第 6 节防回归守卫 | **部分做了但无效**（S0），必须重做 |
| 凭据管理多选按钮 + 卡片多选 + 强制删除 | 批 5 | 未做 |
| OTA 一键升级打通（用户已提供 PAT） | 批 6 | 未做 |
| 用量/额度动态刷新（「用了余额之后要刷新额度显示」） | 批 7 | 未做，**该路 workflow 因 502 失败，需重跑侦察** |
| cache 专题续做（EXP-1/EXP-2） | 批 7 | 未做，**该路 workflow 因 502 失败，需重跑侦察** |

> ⚠️ `usage` 与 `cache` 两路侦察因号池 502 未完成，MASTERPLAN 里这两块的深度低于其它六路。
> 已知线索：`first_token_ms` 疑似**从不写入** `traces.db`（若确认，是独立的可观测性缺陷，
> 会让所有延迟分析失效），`credits_used` 同样需核实写入链。EXP-1 已跑过 8 档但
> `credits_used` / `first_token_ms` / `cache_read_input_tokens` **全为 null**
> （见 `docs/cache-probe-data/observations.jsonl`）。

---

## 5. 待用户决策（不要自己拍板）

MASTERPLAN 第四节有 9 条（D1~D9）。最要紧的三条：

- **D1 「强制删除」的确切语义**：绕禁用门 vs 彻底不进回收站？
  推荐拆成 `force` 与 `purge` 两个独立参数，`purge` 默认 false
  —— adminKey 明文存 localStorage + 全仓无 CSP，回收站是被 XSS 打穿后唯一兜底。
- **D2 OTA 要不要打 Release**：推荐必须走 Release，不要改代码去读 Actions artifact
  （artifact 有 zip 包装、按 run id 而非 tag 寻址、需 actions:read、14 天过期四重不兼容）。
- **D3 affinity TTL 缩到 5min**：推荐先做 EXP-1/EXP-2 再动
  —— 「缩短会掉命中率」这个担忧当前**没有可测量依据**（11% 是估算值）。

---

## 6. 防回归守卫（让「越跑越慢」结构性不再出现）

S0 是这一节存在的理由：**`decay_idle` 是带测试、审查过的修复，
却被同一个函数几行后的一行赋值完全失效。** 靠代码审查保证「每个惩罚状态都有下降路径」
已证明不可靠。MASTERPLAN 第六节有四层（G1~G4），最关键两条：

**G1 元测试 — 只推进时间，且必须走真实读路径**

```rust
#[test]
fn test_all_penalty_counters_have_time_only_recovery() {
    // 造最坏状态：连续 429 打到跳闸、多轮半开失败、族级 403
    // 然后【只推进时间】—— 零成功、零请求
    // 断言：p_avail >= 0.40 ∧ consecutive_429 == 0 ∧ open_count 下降 ∧ consecutive_suspicious == 0
}
```

两条硬规则（建议写进 `CONTRIBUTING.md`）：
1. **禁止在测试里手工写私有状态字段** —— 这正是 S0 漏网的直接原因。
2. 改为**注入时钟**（`Clock` trait，生产 `Instant::now()` / 测试 `MockClock`），
   测试只调公开 API + 推进时钟，永不碰字段。

**G2 反饥饿强制探测 — 把断言变成强制执行**

`sort_key` 加最高优先位 `starved`：任何号超过 `STARVATION_PROBE_SECS` 未被选中
且未 disabled / 未 cooling / 未被 blocklist 挡 → 本轮无条件排首位。

**它的价值不在修哪个 bug，而在于让所有单向状态缺陷的最坏后果降级为
「偶尔损失一个探测请求」** —— S0/S1 即使全都不修，池子也不会出现「有效容量 6→3」。

---

## 7. 环境与验证手段

```bash
# 构建（必须带 --no-default-features：Cargo.toml 的 default=["native-tls"] 与出厂配置相反）
cd admin-ui && pnpm install --frozen-lockfile && pnpm build && cd ..   # rust-embed 编译期需要 dist
cargo test --no-default-features --bin kirostudio        # 当前 924 全绿
cargo clippy --no-default-features

# 出 VPS 二进制（不要在 VPS 上编译）
gh workflow run deploy-build.yml --repo dwgx/KiroStudio-skiapi --ref deploy/vps -f run_tests=true
```

线上观测（**用户已说明：现在不要动 VPS**，以下仅供必要时只读排查）：

```bash
ssh ws-vps '/usr/local/bin/gateway-status'     # 全链路巡检
ssh ws-vps '/usr/local/bin/gateway-status brief'
```

> ⚠️ SSH 当前**只能用密码**（服务器 `PubkeyAuthentication no` 且
> `authorized_keys` 被清空）。非交互场景用 `sshpass`，凭据在
> `~/Documents/WorkSpace/ws-vps/secrets/CREDENTIALS.md`。
>
> ⚠️ `gateway-status` 会**假绿**：它只看「是否在冷却中」而不看「拿去用能否成功」。
> 本轮就出现过它报「43 个号全可用」而实际全在返 403。判断号池真实健康要看
> `traces.db` 的**逐号成功率**，不要信巡检的号数。

---

## 8. 建议的下一步顺序

1. **批 1（纯前端）** — 三处改动：`error && !data`、两个 axios 补 `timeout: 15000`、
   502 重试放宽带退避。不碰后端、不碰号池，用户马上能感知。
2. **部署已写完的 `shutdown_with_drain_cap`** — 顺带把部署中断从 74s 降到 8s
   （这次部署本身就会被它缩短）。
3. **批 3 重做 S0** — 拆衰减时钟 + 去门槛 + 补 S1 的三个棘轮 + 落地 G1/G2 守卫。
   **这是用户最在意的「越跑越慢」，且我上次没做对。**
4. 批 2（S2 封号原因持久化）→ 批 5（多选+强制删除）→ 批 6（OTA）。
5. 重跑 `usage` / `cache` 两路侦察（上次因 502 失败）。

**每批上线后必须在进程持续运行 + 有真实流量的窗口里观测**，
不要在重启后立刻看 EWMA 类指标（见 S0 的教训）。
