# KiroStudio 交接文档 — 2026-07-31

> 给下一个接手的 AI / 人。**先完整读第 0、1 节再动任何代码。**
>
> ⚠️ **本文写完之后又发现了两条致命缺陷（`ksk_` 号走错端点被误禁、403 打死全池后无自愈路径），
> 已在本地修好但未上线。任务清单见同目录 `HANDOFF-NEXT-TASKS.md` —— 先读那份。**
> 本文取代 `HANDOFF-NEXT.md`（2026-07-29）。那份的批 A–E 已有多项完成，状态见第 3 节。
> 配套仍有效：`ECOSYSTEM-STUDY-2026-07-30.md`（生态对标，**结论已可关闭该线**，见 3.6）。

> 🔄 **2026-08-03 修订**：**0.7.46 已上线**（`Cargo.toml` 也已是 0.7.46），上面那两条致命缺陷
> 随之发布。本次修订逐处更正了过期状态与三条错误结论（`LiveCred` 字段数、`snapshot()` 的
> SHA256 次数、`hotswap.sh` 的 `cp -a` 建议），并在第 9 节补了本轮新增的实测事实。
> **原文的实测数字一律保留 —— 那些是这份文档最值钱的部分，只有"状态"和"结论"被更正。**

---

## 0. 六条硬约束（违反会造成真实损失）

1. **工作树有 67 个未提交条目，属多个并行会话。**（2026-08-03：已涨到 **71**。）
   禁止 `git checkout` / `switch` / `stash` / `reset` / `commit` / `add`，禁止全仓 `cargo fmt`。

   ⚠️ **本轮我违反过一次并造成真实损失**：跑 `git checkout-index -f -- src/kiro/health.rs`
   抹掉了另一个会话在该文件里的 **515 行未提交改动**（984 行 → 474 行）。
   已从 `deploy/vps` 完整恢复（那份已推送过），但如果它没被推过就永久丢了。
   **教训：备份只用 `cp` 到 `/tmp`，永远不要用任何 git 写命令去"还原"文件。**

2. **`origin` = `dwgx/KiroStudio-skiapi`（private，唯一开发仓库）。**
   **`public` = `dwgx/KiroStudio`（PUBLIC，已冻结）。** 显式写 remote 名时务必确认不是 `public`。

3. **不要在 VPS 上编译。** 4 核是瓶颈，会抢死正在服务的 sub2api。走 GitHub Actions。

4. **禁止用脚本做批量代码编辑。** 历史事故：python 替换删掉三个函数整段；正则把字段插进 `impl` 块造成 209 个编译错误。
   用 Edit 逐处改，或改前 `cp` 到 `/tmp` 再逐步验证。

5. **不做没有证据支撑的改动。** 判据是：**能否构造出"移除它即失败"的测试**。构造不出就不要留。
   本轮我因此撤掉了自己的一个设计（见 1.3 的 `BadRequest` 那条）。

6. **每条改动都要有能抓住旧 bug 的回归测试。**
   验证方法：把修复处改回旧行为 → 跑测试 → **必须 FAILED** → 再还原。
   本轮 30 条改动全部这样验过；有 3 条我第一次验证方式是错的（见第 5 节），值得读。

---

## 1. 当前状态（2026-07-31 部署后实测）

> 🔄 **2026-08-03**：本节的版本/号池数字已过期。线上与 `Cargo.toml` 均为 **0.7.46**。
> 号池仍然**只剩 1 个号**（这条不但没解决，还是持续状态），近 2h 的真实 outcome 见第 9 节
> —— 与本节「59 请求全 success」那个 5 分钟窗口差距很大，**不要用 5 分钟窗口判断链路健康**。

### 线上

| 项 | 值 |
|---|---|
| 版本 | ~~**0.7.45**（`deploy/vps` = `5af3af51`）~~ → **0.7.46**（2026-08-03） |
| 服务 | active，零空窗交接完成（MainPID 758995 → 2253236） |
| 近 5 分钟 | **59 请求全 success，0 panic** |
| 巡检 | 五条链路 + 证书 + 号池全绿 |
| 号池 | ⚠️ **只剩 1 个号**，65 RPM。交接前是 4/5，过程中掉到 1 —— **不是部署造成的**，是上游风控持续烧号 |

🔴 **最要紧的运维事项：号池无冗余。** 单号一挂即断服，需要补号。
风控烧号速率实测：单号存活 25~60 分钟，两天内凭据 id 从 #223 跑到 #316。

### 本地

```
worktree: branch=fix/macos-support-and-critical-bugs  head=a840c5e  dirty=67
deploy/vps（远端）= 5af3af51
```

**理解 git 拓扑（否则会误判工作丢了）**：`a840c5e` 是 `deploy/vps` 的**祖先**。
`git status` 相对 HEAD 判定，所以 `deploy/vps` 里已有的文件（`cli.rs` / `reasoning.rs` /
`http_read.rs` / `hotswap.sh` / CACHE 三份文档）在 `git status` 里显示为 `??` 未跟踪。
**已逐字节核对：工作树的 `src/` 与已部署代码完全一致。** `git diff deploy/vps` 显示的
"-2103 行" 是 diff 方向造成的假象，不是缺失。

### OTA 已打通（本轮最有价值的交付之一）

```json
{"has_update":false,"local_version":"0.7.45","latest_version":"v0.7.45","error":null}
```

`error: null` 是关键。三个前置全齐：

- **Release**：`v0.7.45` 已建，含 linux-x86_64 / macos-{aarch64,x86_64} / windows-x86_64.exe + 各自 sha256
- **PAT**：写入 `/etc/kirostudio/update.env`
- **加载**：`EnvironmentFile` 启动期读取，已用 hotswap 零空窗重启加载

⚠️ **PAT 是 `gho_` 型（gh CLI 的 OAuth token，取自 `gh auth token`）。
用户下次 `gh auth login` 重新登录后它会轮换 → OTA 失效。**
彻底稳的做法：GitHub 网页建 fine-grained PAT（只勾 `dwgx/KiroStudio-skiapi`，Contents: read），
换掉 env 里那一行再重启。链路本身已验证通，换 token 只是改一行。

**以后发版流程**：bump `Cargo.toml` → 打 tag → 面板点「检查更新」即可升级。

> 🔄 **2026-08-03**：上面那个 JSON 的版本号已是 0.7.46/`v0.7.46`；`error: null` 仍成立。
> `gho_` PAT 的轮换风险**依然未解决**，换 fine-grained PAT 这件事还在待办里。

---

## 2. 本轮完成的 30 条（已上线，全部带回归测试）

### 调度与错误分类

| # | 改的是什么 | 依据 |
|---|---|---|
| 1 | 模型级硬门不再谎报"所有凭据均已禁用"并落 502 | 线上 24h 实测 **577 次**假报（`gpt-5.6-*`/`deepseek`/`glm-5` 各 ~85）。原因：`NoCandidate` 分支对"真耗尽"与"被 `model_blocklist` 挡掉"报同一句话，该串匹配不上 `map_provider_error` 任何分支 → 502 无 Retry-After。现拆为：限时态带 `retry_after_secs` 走 429；**永久态**（订阅档位/成本白名单）走 404 不可重试（否则只是把 502 死循环换成 429 死循环） |
| 2 | 代挂号（custom_api）失败策略按其真实语义重写 | 代挂号是用户自购的付费中转站，**没有"被风控"这个状态**。429 只代表"它现在忙"。现：429 零惩罚（仅 5s 调度级跳过，不进 health/不计失败）、非瞬态连续 3 次自动禁用、持续过载 300s 窗口 |
| 3 | `BadRequest` **完全不计入**代挂号健康 | **实测推翻了我自己的设计**：代挂号 #216 成功率 80.3%（2910/3622）是健康号，却有 712 次 `bad_request`、其中 **119 次 ≥3 连**。原设计会误禁它 119 次。且代挂号历史 429 = 0、失败形态全是 bad_request |
| 4 | MCP 路径补 403 风控分类（E2） | 原先 403 无条件 `report_failure`，3 次即以 `TooManyFailures`（永久型标签）禁用 → 临时限流的号走 WebSearch 被打三次就永久死。与历史 88 次误禁事故同一形态，对话路径修了、这条漏了 |
| 5 | 复活路径清全部瞬态计数 + 全池自愈落盘（E3） | 三条复活路径各自手写清零列表、都漏 `consecutive_suspicious`；自愈只改内存 → 重启回死态 |

### 可观测性

| # | 改的是什么 | 依据 |
|---|---|---|
| 6 | **`first_token_ms` 从未写入 → 现已落库** | 全仓 **0 个生产赋值点**，线上 24h **59458 条全 NULL** → 所有延迟分析失效。打在唯一 choke point `process_kiro_event`（流式与 buffered 共用），加 `generate_final_events` 兜底（整轮文本 hold 到流末的响应不经过 choke point）。`CallMeta.started_at` 与 `latency_ms` **同源**，故 `first_token_ms - latency_ms` = 响应头→首 token。**已验证线上有值** |
| 7 | 聚合层平均 TTFB | `avg_first_token_ms()` 返回 `Option` 而非 0（`first_token_ms` 是 Option，用 `requests` 当分母会把 None 当 0ms 摊进去；显示 "0ms TTFB" 物理不可能，比 "—" 危险） |
| 8 | 封号原因与时刻全链路保真 | 删号进回收站丢三元组（线上 175 条**无一条**带原因）；`restore_credential` 抹成 `Manual`；`disabled_at` 整条链断在最后一跳（`CredentialStatusItem` 与 snapshot 两侧都缺字段，面板恒 null）。**这条是 smoke 测试发现的，单测抓不到** |

### 韧性与边界

| # | 改的是什么 | 依据 |
|---|---|---|
| 9 | 余额查询 6s 超时降级 | 链路 `web_portal` 超时是 30s/60s、中间零降级，而前端 axios 15s → 用户必然先看到失败、后端还在等。现超时返 `stale` 上次已知值 |
| 10 | 余额展示乐观修正 | 真值每 30 分钟刷一次 → 跑完请求额度最多 30 分钟不动。用本地 `total_credits_used` 减缓存基线推进。**绝不为此每请求打 web_portal**（那是上游探测，会加重风控） |
| 11 | `by_model` 有界化 | key 是**外部可控**（透传路径在 `convert_request` **之前**执行，用的是客户端原始 `payload.model`，从未过 `map_model`），且这张表**永不回收**。实测 500 个随机名 → 500 条永久条目。现截断 128 + 上限 256 + `(other)` 桶（归桶而非丢弃，保总量守恒） |
| 12 | `MAX_RECENT_LIMIT` 50000 → 2000 | 实测取 5 万行 **42ms / 6.5MB**（此前假定 0.3ms，差 140 倍）。危害在**持锁**：`TraceDb` 单连接单 Mutex，与用量写管道共享，42ms 持锁会让写侧排队、排满 10000 后**静默丢真实请求记录**。即「点一下面板的『全部』会丢用量数据」 |
| 13 | 三处响应体读取收口 `common::http_read` | `Content-Length` 预检可被 chunked 绕过。原有两份各自实现（OTA、背景图图片），而**同文件的背景图 JSON 那一跳被漏掉** —— 正是"每处各写一份"导致的漏改 |
| 14 | `passthrough` 上游中断改为错误传播 | `Ok(Bytes::new())` 把中断伪装成正常 EOF → 客户端拿 200 + 截断内容判定成功、不重试。**注意：这不是"CPU 忙循环"**，见第 5 节 |

### 回滚安全（🔴 上线前最后一刻发现的 BLOCKER）

| # | 改的是什么 |
|---|---|
| 15 | `DisabledReason` 加 `#[serde(other)] Unknown` |

本轮给该枚举加了 2 个变体（9→11）。新版本一旦把 `passthroughFailed` 写进 `credentials.json`，
**旧版本读不了** → `unknown variant` → `CredentialsConfig::load` 返 Err →
`main.rs` `std::process::exit(1)`（刻意的 fail-safe）。**即回滚 = 服务起不来，且要在生产压力下手工编辑 JSON。**

⚠️ **这条是我自己第一次否证错了的**：第一版复现漏了 struct 级 `#[serde(rename_all)]`，
字段名从未匹配上，"测试通过"其实什么都没测。补上后立刻复现。
**新增任何持久化枚举变体时都要想这一条。绝不要删掉 `Unknown`。**

### 防回归

| # | 改的是什么 |
|---|---|
| 16 | **G1 元测试**（`health.rs`）：造最坏惩罚状态 → 只推进时间 → 断言衰减真实发生 |
| 17 | E1 接入上游结构化 thinking 流（`reasoningContentEvent`） |

G1 的两条纪律：**不手工写私有字段**（旧测试靠回拨 `last_decay_at` 才"通过"，绕过了要验证的读路径）；
**必须走真实读路径且高频反复读**（只读一次 `last_touch` 还是旧值，缺陷不显形）。

判据用「衰减达到理论量级」而非「严格小于」——实测发现只换衰减时钟**不足以**复现 S0
（因为 `decay_penalties` 在 `s.last_touch = now` **之前**执行），真正成因是那个已删除的**门槛**
`if idle < IDLE_DECAY_MIN_SECS { return }`。复现门槛形态时 G1 FAIL 且 `actual = 1.000000`，
与历史"200 轮选号后 ewma 一次未降"吻合。

E1 有个坑：结构化 reasoning **无终止帧**，真实形态是「N 帧 reasoning → 普通正文（无标签）」。
首版让两条路径共享 `in_thinking_block`，导致**正文被路由进思考面板** → 客户端显示空答案。
现用 `reasoning_stream_seen` 使两者互斥（首个正文即关块）。

### 功能

18–20：凭据强制删除（`force` 绕禁用门，**仍进回收站** —— adminKey 明文存 localStorage 且全仓无 CSP，
回收站是被 XSS 打穿后唯一兜底）+ 批量删除端点（此前删 N 个需 2N 次往返）；
`trust_forwarded_header` 接入 handler 层（已知问题 #6）；
前端 `gcTime` 30min（使故障降级不再有 5 分钟有效期）、回收站显示封号原因、三语 i18n。

21–30 为上述各条的配套测试与前后端字段打通。

**质量门**：单测全绿 / clippy 0 error / tsc 干净 / CI `cargo test --locked` 通过。

> ⚠️ 原文写「966 单测」。**不要在文档里写死测试数** —— 它每轮都涨（931 → 966 → 969 → …），
> 写死的那一刻就开始误导人。**一律以 `cargo test --no-default-features --bin kirostudio` 实跑为准。**

---

## 3. 未完成的工作

### 3.1 号池补号（🔴 最紧急，属运维）
只剩 1 个号，无冗余。风控烧号速率见第 1 节。

### 3.2 `p_avail` 批量化 + 瘦 `live_snapshot`（❌ **两项都判定不做**，理由见下）

> 🔄 **2026-08-03 更正 —— 本小节的两个前提都不成立，别照它开工：**
>
> - **`p_avail` 批量化：明确不做。** 没有实测支撑（"43 次锁"从未被量出对延迟的影响），
>   它碰**选号热路径**（错了掀翻分流），且批量化后的行为差异（`decay_carry` 少累加几微秒）
>   **不可观测** → 构造不出「移除它即失败」的测试。按本文自己的硬约束 5，这条就该撤。
>   ✅ 但**其中那条隐患是真的且已修**：`inflight` 在 sort_key 里的两次独立 `load(Acquire)`
>   已收口成单次读（主线本会话）。
> - **瘦 `live_snapshot`：前提是错的。** 原文说 `LiveCred` **只用 id/rpm/inflight**、
>   每号白算**两次 SHA256**、43 号池每 1.5s 白算 86 次 —— **三个数字全错**。
>   实读：`LiveCred` 有 **7** 个字段（id / rpm / inflight / cooling_down / cooldown_remaining_ms
>   / circuit_open / health_score），`snapshot()` 每号只算 **1 次** SHA256
>   （两个分支由同一个 `is_api_key_credential()` 判定、互斥）。
>   即 43 号池是 43 次 SHA256 而非 86 次，而且那些"白算"的字段实际都在用。
>   **收益远小于原文声称的，不值得碰。** 教训：**改优化前先把被优化对象的字段数点一遍。**

- `p_avail_with_load_ref` 每候选一次 `states.lock()`，在 `entries.lock()` 临界区内。43 号池 = 43 次锁。
  范式照抄 `RpmTracker::counts_for`。
- **关键差异**：`counts_for` 键是 `e.id`（唯一），`p_avail` 键是 `family_key`（M365 同租户**会重复**）。
  批量版按去重族键只推进一次 → **与逐个调用不逐位相同**（`decay_carry` 少累加几微秒），
  这是刻意的语义变更，必须在注释写明。
- **已存在的隐患**：`inflight` 在 sort_key 里是**两次独立** `load(Acquire)`，p 值用旧值、另一排序位用新值。批量化时应统一成一次读。
- 瘦 `live_snapshot`：`live_creds` 每 1.5s 调 `snapshot()`，而 `LiveCred` **只用 id/rpm/inflight**，
  却白算每号**两次 SHA256** + 十几个字段 clone。43 号池每 1.5s 白算 86 次 SHA256。
  **这条收益比 SSE broadcast 化本身更大，且两者独立。**
- ⚠️ `p_avail` 碰**选号热路径**（错了会掀翻分流），`live_snapshot` 只碰展示路径。**必须拆两个 commit**，否则出问题无法二分定位。

### 3.3 SSE `stream_live` 改 O(1)
每标签页各自 1.5s 拉快照 = O(标签页)。**建议用 `watch` 而非 `broadcast`**：
live 帧语义是"只要最新的"，watch 天然无 `Lagged`、慢消费者不积压。
陷阱：`AdminState` 必须持一个常驻 `Receiver`（否则所有 SSE 断开后 channel 关闭），
因此门控阈值是 `receiver_count() <= 1` 而非 `== 0`。

### 3.4 cache EXP-1 / EXP-2
**硬依赖真实流量**。TTFB 埋点已完成并验证有值，现在可以跑了 ——
需要线上积累一段样本后分析。先读 `docs/CACHE-EXP0-RESULT.md`。

### 3.5 明确判定「不要做」的两项（附理由，防止下一个人重做）

- **不要加 SQLite `busy_timeout`**：rusqlite **已无条件默认设 5000ms**
  （`inner_connection.rs:118`，我读了源码确认）。而且 `TraceDb` 是**单连接 + 一把 Mutex**，
  SQLite 层压根不争锁，该 PRAGMA 在本进程内是死代码。
  MASTERPLAN 那条"避免锁等待直接返 SQLITE_BUSY"的**前提不成立**。
- **不要给 trace_db 查询包 `spawn_blocking`**：真正的争抢在 `TraceDb.conn` 那把
  `parking_lot::Mutex` 上，`spawn_blocking` 只是换个线程占锁。已用**降 `MAX_RECENT_LIMIT`** 解决。

### 3.6 上游对照线可以关闭

逐条比对 `hank9999/kiro.rs` 的 28 条 fix 提交，**零可修项** —— 上游修过的我们全都有，
且 4 处比它更严；而上游自己还留着两个我们已修的缺陷（`&session_part[..36]` 会 panic、
全局自愈不落盘）。分叉 4.07 倍（62347 vs 15306 行）。
⚠️ 该 clone 是 **shallow**（`.git/shallow` = `08b2b9f`，2026-03-18），结论只覆盖该窗口之后。
唯一剩余价值：上游加新模型时照抄一条 `ModelSpec`。

### 3.7 已记录未修的小项

- ~~`hotswap.sh:146` 回滚用 `mv "${BIN}.prev"` —— **`mv` 会吃掉回滚点**，应为 `cp -a`~~
  ✅ **已修（主线本会话），但原文给的修法是错的**：`cp -a` 覆盖**正在运行的**二进制会报
  **ETXTBSY**（Text file busy）→ 回滚在最需要它的时刻失败。正解是 `install`
  （它先 unlink 目标再写新 inode，运行中的进程继续持有旧 inode，不受影响）。
  **"`mv` 吃掉回滚点"这个诊断是对的，只有开的方子错了。**
- ~~`hotswap.sh` 无 `trap`~~ ✅ **已修（主线本会话，加了 `trap`）。**
  原文对危害的描述仍然准确、值得保留：中途 Ctrl-C 会留**孤儿裸实例**常驻同端口，
  持续分流真实流量且不受 systemd 管理，还会与新实例双写 `kiro_stats.json`（last-writer-wins）
- 上游三处刷新请求带 `Connection: close`，我们不带。**是否构成指纹差异只能靠抓包定**，代码里看不出

---

## 4. 部署与回滚（本轮实测走通）

### 部署（零空窗）

```bash
# 1. bump 版本（release.yml 有 tag/Cargo.toml 一致性门禁，不 bump 打 tag 会被拒）
# 2. plumbing 快照提交，基树 origin/deploy/vps —— 绝不碰真实 index
export GIT_INDEX_FILE=/tmp/snap.index && rm -f "$GIT_INDEX_FILE"
git read-tree origin/deploy/vps
git add -A -- src admin-ui/src deploy Cargo.toml Cargo.lock
TREE=$(git write-tree); C=$(git commit-tree $TREE -p origin/deploy/vps -m "...")
git branch -f deploy/vps $C && unset GIT_INDEX_FILE
git push origin deploy/vps
# 做完必须核对：分支名与 git status --porcelain | wc -l 与开始时一致

# 3. CI 出二进制
gh workflow run deploy-build.yml --repo dwgx/KiroStudio-skiapi --ref deploy/vps -f run_tests=true
gh run download <RUN_ID> --repo dwgx/KiroStudio-skiapi -D /tmp/dl
# 三处 sha256 必须一致：CI 产物 / 本地 / 服务器

# 4. 交接（先 check 不换，再真换）
scp <bin> ws-vps:/tmp/kirostudio.new && scp deploy/hotswap.sh ws-vps:/tmp/
ssh ws-vps '/tmp/hotswap.sh /tmp/kirostudio.new check'   # 旧实例完全不受影响
ssh ws-vps '/tmp/hotswap.sh /tmp/kirostudio.new'
```

**验证点（缺一不可）**：

1. `pgrep -c -f /opt/kirostudio/bin/kirostudio` 应为 **1**（排除孤儿裸实例）
2. **真实流量**：发一个真 `/v1/messages`（非流式 + 流式各一）。
   ⚠️ `hotswap.sh` 的健康检查只打 `/v1/models`，**它不碰号池、不碰上游** ——
   选号/流式的回归它抓不到，必须自己发真请求
3. `traces.db` 近 5 分钟 outcome 分布 + `journalctl | grep -c panicked`

### 回滚

**三层备份都在**：

- `kirostudio.rollback-0744` ← 我另存的**持久**回滚点
- `kirostudio.prev` ← hotswap 自动建的（⚠️ `kirostudio-update rollback` 用 `mv`，**用一次就没了**
  —— `deploy/hotswap.sh` 侧已改用 `install` 修掉，见 3.7；VPS 上的 `kirostudio-update`
  脚本在 `ws-vps` 仓库里，**那份仍是旧行为**）
- `credentials.json.pre-0745` / `config.json.pre-0745` / `trash.json.pre-0745`

```bash
ssh ws-vps '/tmp/hotswap.sh /opt/kirostudio/bin/kirostudio.rollback-0744'   # 零空窗
```

**回滚起不来时**：查 `credentials.json` 是否含新枚举变体（见 2.15）。
0.7.45 已加 `serde(other)` 兜底，从 0.7.45 往后回滚是安全的；**从 0.7.45 回到 0.7.44 仍有此风险**
（0.7.44 没有兜底）—— 那正是 `credentials.json.pre-0745` 存在的理由。

---

## 5. 本轮我犯的错（这一节最值得读）

1. **`git checkout-index` 毁掉别人 515 行**（见约束 1）。已恢复。**根因是我用 git 写命令去"还原"文件。**
2. **回滚 BLOCKER 我第一次否证错了**：复现代码漏了 struct 级 `#[serde(rename_all)]`，
   字段名从未匹配，"测试通过"其实什么都没测到。**教训：测试"通过"时要问它是否真的执行到了被测路径。**
3. **G1 元测试失败三次才做对**：前两次都是"机制推理正确但复现方式不对"。
   第三次我停止猜测、写诊断脚本把两种实现**并排实测**，才发现真正成因与假设不同。
   **同一前提失败两次就该停下来量，而不是继续改测试。**
4. **`BadRequest` 那条设计是我自己错的**，靠查线上 traces 才发现会误禁健康号 119 次。
   **写完策略要拿真实数据回测，不要只靠语义推理。**
5. **有一条我先前反复说"只能你来"是错的**：OTA 的 PAT 其实早就在 `gh auth token` 里，
   用户提醒后才发现。**声称"我做不了"之前先真的找一遍。**

另外记一条**不是我的错但极易重犯**的：`passthrough.rs` 的 `Ok(Bytes::new())` 曾被诊断为
"CPU 忙循环把日志刷到 79GiB"，机制讲得很顺。**实测证伪**：reqwest 的 `bytes_stream` 出错后
下次 poll 返回 `None`（不重复吐 Err），`.map()` 也不改终止时机 —— 三种断连方式全部 2 次 poll 内终止。
它是**正确性缺陷**（把中断伪装成成功），不是自旋。**"机制听起来成立" ≠ 成立。**

---

## 6. 环境与工具

```bash
# 构建（必须带 --no-default-features：Cargo.toml 的 default=["native-tls"] 与出厂配置相反）
cd admin-ui && pnpm install --frozen-lockfile && pnpm build && cd ..   # rust-embed 编译期需要 dist
cargo test --no-default-features --bin kirostudio      # 全绿（数量以实跑为准，别写死）
cargo clippy --no-default-features --bin kirostudio    # 0 error
cd admin-ui && npx tsc --noEmit                        # 干净
```

⚠️ **缺 `admin-ui/dist` 时 cargo 报 E0599** `no associated function named 'get'`（不是友好报错）。

⚠️ **macOS 上 `cp` 二进制到别处会被 SIGKILL**（exit 137，ad-hoc 签名被 cp 破坏）。
本轮踩过：本地 smoke 必须**从原路径**运行 `./target/release/kirostudio`。

⚠️ **测试里用 reqwest 打 127.0.0.1 必须 `.no_proxy()`**：本 crate 开了 `system-proxy` feature，
开发机装 Clash/Surge 时会把本地请求塞进代理隧道，报 hyper `IncompleteMessage`
（看起来像"连接坏了"，与被测逻辑无关）。与已知问题 #19 同型。

### fan-out subagent 的注意事项

agent 调用走的正是 `k1ro.skiapi.dev`（我们自己的网关）。号池紧张时**8 路并行必然把它打穿** ——
本轮 fleet 被 502 打死 **5 次**，交接文档里也记过两次。
**建议：agent 只做只读侦察，并发 ≤3；写代码由主线串行做**（工作树有别人的未提交改动，并行写会毁掉它们；并发 cargo 还会抢 target 锁）。

### 线上只读排查

```bash
ssh ws-vps 'gateway-status'          # 全链路巡检
ssh ws-vps 'gateway-status brief'
```

> ✅ **2026-08-03：SSH 密钥登录已恢复**，`ssh ws-vps` 直接可用。
> `CLAUDE.md` 与 `HANDOFF-NEXT.md` 里「SSH 只能用密码」那段已过期，`sshpass` 只作备用。

⚠️ **`gateway-status` 会假绿** —— 它只看"是否在冷却中"而不看"拿去用能否成功"。
判断号池真实健康要看 `traces.db` 的**逐号成功率**。

---

## 7. 线上配置（刻意偏离默认值，不要"按默认更安全"改回去）

完整依据在 `ws-vps/docs/02-tuning.md`。本轮**零新增配置项**（`config.rs` 相对线上 diff 为空）。

> 🔄 **2026-08-03 实读线上 79 项配置，本表三处真值与记录不符 —— 以下为真值**：
>
> | 配置 | 本表记的 | **线上真值** | 说明 |
> |---|---|---|---|
> | `inboundThrottleEnabled` | false | **true** | `inboundTargetRpm=133`，由 `throttle-autotune.timer` 每 2 分钟自动调。但见下面「容量口径是假的」—— 133 实际什么都没限住 |
> | `cooldownEnabled` | false | **false** ✅ | 这条本表是对的，且「死号自动禁用已不依赖它」的注解仍有效 |
> | `ccAutoBuffer` | false | **true** | ⚠️ **两条实测证据链互相矛盾（省 774ms vs 计费/精度侧的观测），需用户拍板，不要自行改任一方向。** |
>
> 其余相关真值：`inboundBurstSecs=2` / `inboundQueueMaxWaitSecs=30` /
> `inboundQueueTimeoutPassthrough=true` / `inboundRpmMin=50` / `inboundRpmMax=300` /
> `inboundRpmAuto=false`（本表这条正确）/ `rpmHeadroomFactor=85` / `rpmReserveSlots=3` /
> **`rpmHardGateOverloadWait=true`**（注意 `CLAUDE.md` 记的是 false，已过期）/
> `affinityEnabled=true` / `promptCacheEnabled=true` / `promptCacheTtlSeconds=3600` /
> `overloadFallbackModel=None` / `ingressRateLimitPerMin=0`。
>
> **表里所有实测数字（+0.09 spearman、6.4s 一次 429、774ms、令牌桶容量塌陷）仍然有效。**
> 变的只是开关当前处于哪一档。

| 配置 | 线上值 | 为什么不能改回 |
|---|---|---|
| `rateLimitEnabled` | false | 每号最小间隔 1000ms 会在 241ms 处踢开亲和绑定 → 每次换号 → prompt cache 全丢。5339 样本实测速率与 429 率 spearman 仅 **+0.09** |
| `inboundRpmAuto` | false | 内置 AIMD 是单向棘轮：429 砍半、回升要 20s 静默 ×N，而实测每 6.4s 一次 429 → 锁死在下限（实测卡 30 RPM 而号池能跑 216）。低档还会触发令牌桶容量塌陷 |
| `inboundThrottleEnabled` | false | 吞吐上限改由号池真实容量决定 |
| `ccAutoBuffer` | false | 实测省 774ms，且四种计费形态 `input_tokens` 完全一致 |
| `cooldownEnabled` | false | ⚠️ 注意：死号自动禁用**已不依赖它**（那是本轮之前修的） |
| `trustForwardedHeader` | false | sub2api 的透传白名单不转发 XFF → 开了也拿不到真实 IP，而 `client_ip` 恒为服务器自身地址，按它配 IP 黑名单会**一封封掉全部流量** |
| `credentialRpmLimit` | **200** | ⚠️ **这个值与实测不符**：逐 10 分钟观测显示 2 号总量 40~54 RPM 时 429 率 0%，即单号干净吞吐仅 **25~30 RPM**，而配 200 × headroom 80% = 160。早前调优时是 50。**这是个待验证的可疑变量**，但改它要控制实验，我没做 |

> 🔴 **2026-08-03 —— 上面这条怀疑已经坐实，而且比原文说的更严重：整个容量口径是假的。**
>
> `throttle-autotune` 的日志写着「target=133 已接近建议值 133（池容量 **167**，可用 1 个）」。
> 那个 167 不是测出来的，是 `credentialRpmLimit=200 × rpmHeadroomFactor 85%` **算出来的**。
> 实测对照：单号 RPM 峰值 **144**，其中 **17.2% 是 rate_limited**；本表说的"干净吞吐 25~30"
> 是 *429 率为 0 时* 的 RPM，两个数根本不是同一口径。
>
> 后果：整形设在 133 **实际什么都没限住**（真瓶颈是上游 429），而 `throttle-autotune`
> 每 2 分钟拿这个假数字去"自动调节" → **在算空气**。
> 用户抱怨「没法调」的根源不是选项太多，而是**一个关键数字是假的**，
> 所有依赖它的自动调节都失去意义。**修容量口径优先于加任何新旋钮。**

---

## 8. 建议的下一步顺序

> 🔄 **2026-08-03 更新后的顺序**（原清单保留在下方，第 5/6 条已作废/已完成）：
>
> 1. **补号**（运维，仍是最紧急 —— 号池**还是只剩 1 个**，这条从 7-31 拖到现在）
> 2. **修容量口径**（见第 7 节末尾的 🔴）—— 它是「配置合理化」的前置，
>    在假数字上加旋钮只会让"没法调"更严重
> 3. **把 shield 能力融入内置**（用户明确诉求，见 9.2）。硬约束：
>    与既有调度开关**不能冲突**；只能在**尚未向客户端写出任何字节**时重试（正确性红线）；
>    `call_api_with_retry` 返回**未消费的** `(reqwest::Response, CallMeta)` → 这条边界结构性成立。
>    ⚠️ 已核实的两个坑：`acquire_admission()` **在 `call_api_with_retry` 内部**
>    （注释明写"整个客户端请求只过一次"），45s 闸门也相对 `call_started`（同样在内部）
>    —— 在外层加重试循环会把这两条语义悄悄改掉。
> 4. **补 retries 的聚合可观测性**（见 9.4）：`Aggregate` 加 retry 字段 + `add` 读 `r.retries`；
>    admission 超时那条 bail 补 `emit_record` / 计数器（现在**面板完全隐形**）
> 5. **cache EXP-1/EXP-2** —— TTFB 埋点已就绪
> 6. `stream_live` 改 `watch`（3.3，唯一还剩的批 A 项）
> 7. **换 OTA 的 PAT** 为 fine-grained（当前 `gho_` 会随 gh 重新登录轮换）

1. **补号**（运维，最紧急 —— 只剩 1 个号无冗余）
2. **观察 0.7.45** 在真实流量下跑一段：重点看 `first_token_ms` 分布、
   `所有凭据均已禁用` 类错误是否真的降下来（应从 577 次/24h 显著下降）、代挂号是否不再被误禁
3. **换 OTA 的 PAT** 为 fine-grained（当前 `gho_` 会随 gh 重新登录轮换）
4. **cache EXP-1/EXP-2** —— TTFB 埋点已就绪，现在可以做了
5. ~~`p_avail` 批量化 + 瘦 `live_snapshot`（拆两个 commit）~~ ❌ **两项都判定不做，见 3.2**
6. ~~`hotswap.sh` 的 `mv` → `cp -a` 与 `trap` 补齐~~ ✅ **已修，但用的是 `install` 不是 `cp -a`，见 3.7**

**每批上线后必须在进程持续运行 + 有真实流量的窗口里观测。**
不要在重启后立刻看 EWMA 类指标 —— 重启会把它们重置成乐观初值 1.0，
历史上有人因此把重启效果误报成修复效果。

---

## 9. 2026-08-03 新增的实测事实（本文写完之后才有的）

### 9.1 真实 outcome 分布：失败面的大头不是 429，是 403

线上近 2h：**success 59.5% / auth_failed 22.3% / rate_limited 17.2% /
server_error 0.8% / bad_request 0.2%**，avg latency 4416ms、avg TTFB 6916ms，号池仍只剩 1 个号。

全部 auth_failed 都是 `Your User ID (...) temporarily is suspended`，且是**突发**形态：
13:50 一次 928 条、14:50 一次 516 条、**中间为 0**。这个形态说明是**风控窗口开合，不是真封号**
—— 若真封号会是持续而非脉冲。

原先它落 403 → 按 4xx 语义**客户端和外挂都不重试** → 一个本来 20 秒后就能过的请求被判死。
✅ 主线已修：新增 `is_upstream_temporarily_suspended`，分类为 **429 + `Retry-After: 20`**
（`UPSTREAM_SUSPENDED_RETRY_AFTER_SECS`）+ 2 个测试。

**教训：第 1 节那个「近 5 分钟 59 请求全 success」的窗口太短，完全掩盖了这个 22.3%。**

### 9.2 外挂 shield 的性价比已量化：11.6 次重试救回 1 个请求

线上真实链路是 客户端 → Caddy(:443) → **`kiro_shield.py`(:8993，239 行 Python 外挂)** →
KiroStudio(:8990) → Kiro 上游。它的参数与累计统计：

- `RETRYABLE={429,500,502,503,504}`，4xx 不重试；`MAX_BUDGET=600s`、`MAX_ATTEMPTS=60`
- **`MIN_DELAY=1.0`** —— 号池 50ms 就恢复了它也要睡满 1 秒
- backoff：有 `Retry-After` 则 clamp[1,15]，否则 `1.0*1.7^(n-1)` clamp 12
- 预算耗尽返 **503 而非 429**（Cursor 见到 429 会掐会话）
- **累计 requests 22448 / retries 19226 / absorbed 1657 / gave_up 325
  → 11.6 次重试才救回 1 个请求**
- 每次重试是**整请求重打 KiroStudio**，而网关内部还有 12 次换号（`ABSOLUTE_MAX_TOTAL_RETRIES=12`）
  → **真实放大上限 60×12**
- 统计只在它自己进程内 → **面板完全看不见**

用户诉求（原话转述）：把 shield 能力**融入**内置，与后台已有调度开关**不能冲突**，
「必须是完美的逻辑好很好地控制」；要「一个非常合理完美的配置」；
且「**不能非常非常保守，那都运行不好，我这个就是大型中转**」。

### 9.3 容量口径是假的

见第 7 节末尾的 🔴 —— 这是配置体系的核心错误，也是「没法调」的真正根源。

### 9.4 失败记录的 `retries` 恒为 0（已修一半）

实测按 outcome 切开：**所有失败 outcome 无一例外 `retries=0`**
（auth_failed 1487 / rate_limited 1098 / server_error 118 / bad_request 91），
同期 success 有 11 条 `retries=1`，全库历史到过 7+。
即**重试次数只在成功路径落库，失败面完全看不见** —— 而失败面恰恰是唯一需要看重试的地方。

✅ 已修：`provider.rs` 加循环外计数器，`fail_record.retries = attempts_used` + 源码级守卫测试。

⚠️ **仍未修的两个可观测性缺口**：

- `Aggregate`（`usage_stats.rs:79-112`）**无任何 retry 字段**，`add`(114-137) 不读 `r.retries`
  → retries 只能逐条看，**无法聚合、无法画趋势**。面板的重试展示也只有 2 处逐条详情
  （`usage-page.tsx:785/823`、`ops-detail-dialogs.tsx:683`），**无聚合视图**。
  另注意前端类型有**两份独立定义**（`api.ts:679`、`ops.ts:183`），改字段要同时改两处。
- **admission 超时那条 bail（`provider.rs:783` 一带）既不 `emit_record` 也不 bump 任何计数器**
  → 请求在入站整形处被丢掉，**面板上完全不存在这个请求**。
  `recovery_metrics.rs:88` 虽有 `failover_exhausted`，但 bump 点被 `if real_failover_happened`
  包着 → 墙钟预算 break 时若只打过 1 个号也不计入。

### 9.5 主线本会话已修的其余项（不要重做）

号池真耗尽 bail 带 `retry_after_secs`（`token_manager.rs` 两处）；
`compute_max_retries` 的 doc 注释改对 + 定时炸弹测试重写（**本文之前的版本记录它已正确，是错的**）；
sort key 里 `inflight` 双读收口成单读；两处注释漂移（排序键概览 6→10 项、已删除的 jitter 键）；
`hotswap.sh` 的 `install` + `trap`（见 3.7）；凭据多开（`copies` 字段 /
`add_credential_allowing_duplicate` / `MAX_CREDENTIAL_COPIES=16` + 6 测试）。

### 9.6 已明确判定「不做」的清单（有依据，别重提）

`p_avail` 批量化 + 瘦 `live_snapshot`（见 3.2）；SQLite `busy_timeout`（见 3.5）；
给 trace_db 包 `spawn_blocking`（见 3.5）；上游 `kiro.rs` 对照（见 3.6，28 条 fix 零可修项）；
**`ccAutoBuffer` 任一方向的改动 —— 两条实测证据链矛盾，需用户拍板。**
